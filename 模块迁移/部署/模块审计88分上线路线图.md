# BladeX 园区系统 · 模块审计 88 分上线路线图

> 版本：v1.0  
> 建立日期：2026-07-31  
> 基准提交：`71d1cf5`（Harden contract tenancy and finalize termination settlement）  
> 权威原则：本文件是后续分批审计、整改、复验和提交的唯一进度源；每批完成后由用户提交代码，未提交前不得进入下一批。

---

## 0. 作战指挥板

### 0.1 当前位置

| 项目 | 当前值 |
| --- | --- |
| 当前批次 | **第 9 批：流程、模板打印与通知专项** |
| 当前状态 | 🧪 代码审计与整改已完成并提交，待真实多角色专项 UAT；第 1、7 批按用户要求跳过 |
| 代码审计进度 | 7 / 8（当前纳入范围，第 10 批待启动） |
| 正式验收进度 | 0 / 8；第 2 至第 9 批仍待用户 UAT |
| 当前基准提交 | `2799567` |
| 下一动作 | 使用管理员、招商/经理、财务和领导账号复验第 9 批各类流程入口、租客/企业字段回填、审批节点待办、模板预览下载和通知链路；UAT 通过后启动第 10 批最终上线验收 |

### 0.2 批次进度速览（权威状态源）

| 批次 | 审计范围 | 状态 | 评分 | Commit | 完成日期 |
| --- | --- | --- | --- | --- | --- |
| 1 | 全局安全、接口权限、日志与流程基础 | ⏸️ 用户要求暂缓 | 待复评 | — | — |
| 2 | 园区管理 | 🧪 待用户 UAT | 89（代码复评） | — | — |
| 3 | 入驻管理 | 🧪 待用户 UAT | 91（代码复评） | — | — |
| 4 | 合同管理 | 🧪 待用户 UAT | 89（代码复评） | — | — |
| 5 | 逾期管理 | 🧪 待用户 UAT | 89（代码复评） | — | — |
| 6 | 企业服务 | 🧪 待用户 UAT | 89（代码复评） | — | 2026-07-31 |
| 7 | 协同办公 | ⏭️ 用户要求跳过 | — | — | — |
| 8 | 首页、菜单与全局 UI | 🧪 待用户 UAT | 88（代码复评） | — | 2026-07-31 |
| 9 | 流程、模板打印与通知专项 | 🧪 代码审计完成 / 待专项 UAT | 89（代码复核） | `2799567` | 2026-08-03 |
| 10 | 安全、性能、部署与最终验收 | ⏳ 待启动 | — | — | — |

> 状态图例：✅ 已完成并已提交；📦 待用户提交；🧪 待用户 UAT；🚧 进行中；⏳ 待启动；⏭️ 用户要求跳过；⛔ 阻塞。

### 0.3 上线硬门槛

每一个二级菜单及专项批次必须同时满足：

1. 综合评分 **≥ 88 分**。
2. 安全、逻辑健壮性单项分别 **≥ 90 分**。
3. 架构、BladeX 规范、性能、代码质量各项不得低于 **80 分**。
4. 🔴 P0 问题为 **0**。
5. 🟠 P1 问题为 **0**；确实无法在本期完成的，必须有责任人、截止日期、补偿措施和书面上线豁免。
6. 核心业务、权限、数据回流、异常路径必须有真实账号和真实数据复验证据。
7. 整体上线结论取所有二级菜单和专项中的**最低成绩**，不得用平均分掩盖低分模块。

### 0.4 评分模型

| 维度 | 权重 | 最低要求 |
| --- | ---: | ---: |
| 安全 | 25% | 90 |
| 逻辑健壮性 | 20% | 90 |
| 架构合规 | 20% | 80 |
| BladeX 规范 | 15% | 80 |
| 性能 | 10% | 80 |
| 代码质量 | 10% | 80 |

综合评分计算：`安全×25% + 健壮性×20% + 架构×20% + 规范×15% + 性能×10% + 质量×10%`。

---

## 1. 每批固定执行流程

1. **范围盘点**：建立一级菜单、二级菜单、页面、接口、表、流程、模板、角色矩阵。
2. **六维审计**：按安全、健壮性、架构、BladeX 规范、性能、质量逐项检查，所有发现必须定位到文件和行号。
3. **P0/P1 修复**：先清 P0，再清 P1；数据库、公开 API、流程节点等重大变更先记录决策。
4. **自动验证**：后端执行 `cd Boot && mvn clean compile -DskipTests`；表格 UI 有变更时执行 `cd saber3 && pnpm check:business-table-ui`。
5. **用户 UAT**：由用户使用真实角色、真实园区和真实业务数据完成页面与流程验证。
6. **重新评分**：按固定权重出具六维评分、剩余风险及上线结论。
7. **证据回填**：同步本路线图及《上线前验收清单》，记录接口、账号角色、样本数据和结果。
8. **批次停点**：状态改为“📦 待用户提交”，列出建议提交信息并停止；用户确认已提交后才进入下一批。

---

## 2. 分批实施计划

### 2.1 第 1 批：全局安全、权限、日志与流程基础

**状态**：⏸️ 用户要求暂缓  
**目标**：消除可影响全部业务模块的共性上线阻断项，为后续业务模块审计建立可信安全基线。

**范围（In）**：

- OAuth2/JWT、Token、参数签名、接口加解密及安全配置。
- dev/test/prod 环境中的数据库、Redis、OSS、Docker 仓库等敏感配置。
- 登录页默认账号密码及前端敏感信息。
- 通用接口权限、数据权限、多租户/园区隔离的代码基线。
- 关键写操作日志、异常日志和 Flowable 待办/身份映射基础。
- 安全部署说明、环境变量模板及必要的静态扫描规则。

**范围（Out）**：

- 各业务模块特有的字段、流程和 UI 整改，进入对应后续批次。
- 短信、邮件、小程序第三方渠道正式接入，进入第 9 批。
- 生产环境真实密钥写入仓库；仓库只保留环境变量引用和安全示例。

**任务清单**：

- [ ] **1-1 安全配置盘点**：定位硬编码密钥、默认密码、宽松开关、公开接口和日志缺口。
- [ ] **1-2 敏感配置治理**：生产/测试凭据改为环境变量注入，移除 POM 和前端默认凭据。
- [ ] **1-3 接口权限基线**：核验通用搜索、业务写接口及资源归属校验，修复明确越权点。
- [ ] **1-4 审计日志基线**：关键新增、修改、删除、审批、收付款操作具备可追溯日志。
- [ ] **1-5 流程身份基线**：核验 Flowable 待办按账号/角色可见，避免空待办和跨角色可见。
- [ ] **1-6 参数签名决策**：确定签名覆盖范围、兼容接口、前端签名能力和灰度启用方案。
- [ ] **1-7 自动验证与复评**：完成编译、静态扫描、六维复评并更新验收证据。
- [ ] **1-8 用户 UAT**：普通角色跨园区、跨角色、未授权接口和流程待办由用户真实验证。

**验收标准**：

- 仓库不再包含可直接使用的生产/测试数据库、Redis、OSS、Docker 仓库密码。
- 登录页不预填可用管理员密码。
- 非公开接口具有认证基线，关键资源访问校验租户/园区/角色归属。
- 关键业务写操作及异常可通过日志追踪到操作人、时间和业务主键。
- 参数签名有明确方案和生效范围，不以破坏文件上传、预览、下载和第三方回调为代价。
- 后端编译通过；本批综合评分达到 88 分硬门槛。

**当前已知风险**：

- `BladeConfiguration` 当前关闭参数签名，直接全局开启可能使旧前端、文件接口和回调接口失效。
- 历史配置中出现过真实测试/生产凭据，代码移除后仍需轮换已暴露凭据。
- 权限注解不能替代数据归属校验，后续仍需按模块用普通账号做越权复验。

### 2.2 第 2 批：园区管理

**状态**：🧪 代码整改完成，待用户 UAT

审计园区档案、建筑管理、楼层管理、租控管理、房源详情、资产台账、智能硬件、水电记录、车辆及备忘；重点验证楼层增量不重排历史数据、面积口径、房态统计、园区隔离、查询性能和统一表格 UI。

**任务清单**：

- [x] **2-1 范围盘点**：完成园区档案、建筑、楼层、房源、租控、资产、智能设备、水电和车辆的页面、接口、表及权限边界盘点。
- [x] **2-2 园区数据隔离**：园区、建筑、楼层、房源、资产、设备、水电、车辆和租控树统一按账号所属园区收口，详情及写操作校验资源归属。
- [x] **2-3 数据完整性**：补齐园区/建筑/房源重复校验、状态与面积校验、删除引用保护；楼层和房源编辑禁止迁移既有物理位置。
- [x] **2-4 面积与房态口径**：出租率统一按面积计算；除空置外其余七种房态均计入已出租面积；可租面积改为系统动态计算。
- [x] **2-5 资产与智能设备**：资产、设备详情/列表/统计/写操作均校验园区归属，位置关联复用受控的楼宇和房源查询。
- [x] **2-6 水电与车辆闭环**：上次读数由系统锁定读取，用量按倍率计算并回流设备读数；删除抄表记录回退设备读数；车辆类型和状态与前后端、建表口径一致。
- [x] **2-7 权限、UI 与自动验证**：补齐水电/车辆查询、新增、删除按钮权限；园区内嵌表格按统一边框、居中、操作列和分页口径整改；后端编译、表格检查和差异检查通过。
- [ ] **2-8 用户 UAT**：49 号权限 SQL 已在本地执行；使用真实管理员和普通园区账号验证正向流程、跨园区反向路径、数据回流及关键接口耗时。

**代码复评分（UAT 前）**：

| 维度 | 分数 | 结论 |
| --- | ---: | --- |
| 安全 | 92 | 普通账号园区数据隔离及按钮级权限已落代码；49 号权限 SQL 已在本地执行，测试/生产库待部署执行 |
| 逻辑健壮性 | 91 | 面积、状态、重复、引用、抄表回流及物理位置保护已补齐 |
| 架构合规 | 88 | 园区归属校验集中到统一服务，业务 Service 负责规则闭环 |
| BladeX 规范 | 88 | Controller 使用 `@PreAuth`，Service 事务与 Mapper 分层清晰 |
| 性能 | 82 | 已避免部分无范围全量查询；合同派生房态聚合仍需用户结合真实数据量记录耗时 |
| 代码质量 | 88 | 状态常量口径统一，自动检查通过；仍需真实数据回归验证历史兼容性 |
| **综合** | **89** | 达到代码门槛；本地权限 SQL 已完成，因真实角色 UAT、测试/生产 SQL 和性能证据未完成，暂不判定上线通过 |

**自动验证证据（2026-07-31）**：

- `cd Boot && mvn clean compile -DskipTests`：825 个源文件完成编译；后续复核 `mvn compile -DskipTests` 明确返回 `BUILD SUCCESS`。
- `cd saber3 && pnpm check:business-table-ui`：业务表格 UI 检查通过。
- `git diff --check`：通过。

**用户 UAT 清单**：

1. 普通园区账号篡改 `parkId`、建筑/楼层/房源/资产/设备 ID 时不能读取或修改其他园区数据。
2. 平台管理员可以正常跨园区查看和切换，普通账号未绑定园区时收到明确错误。
3. 增加建筑楼层数只新增缺失楼层，原楼层 ID、面积和房源不被重排；编辑楼层不能迁移楼宇或楼层号。
4. 空置以外七种房态全部计入已出租面积，园区、建筑、楼层和租控看板出租率一致。
5. 可租面积随房态自动变化，建筑表单中的可租面积不可手工篡改。
6. 同园区园区/建筑编码与名称、同建筑房间名称重复时被拒绝；有引用的数据不能删除。
7. 水电记录上次读数不可手填，新增后设备当前读数同步；删除最新记录后设备读数回退。
8. 水电和车辆按钮按角色权限显示，直接调用未授权接口返回无权限；车辆摩托车与停用状态可正常保存。
9. 无真实接口的“备忘提醒”页签不再显示。
10. 记录楼层列表、租控看板、园区/建筑统计在真实数据量下的接口耗时。

### 2.3 第 3 批：入驻管理

**状态**：🧪 代码整改完成，待用户 UAT

审计商机管理、背景调查、项目审批、入驻审核、客户维护；重点验证企业名称去重、园区字段、人工核验留痕、驳回重提、Flowable 待办、项目审批表数据回流及权限隔离。

**任务清单**：

- [x] **3-1 范围盘点**：完成商机、背景调查、入驻审核、客户维护、客户标签、Flowable 待办及入驻审批表的页面、接口、表、流程和权限边界盘点。
- [x] **3-2 园区与接口隔离**：商机、客户、背景调查、旧审批项目和客户标签统一按账号所属园区查询，详情、写操作和 URL 篡改均校验资源归属。
- [x] **3-3 企业去重与数据一致性**：商机企业名称全局去重、客户企业名称同园区去重；并发冲突转换为业务提示；商机、客户和标签园区归属禁止通过普通编辑迁移。
- [x] **3-4 背景调查留痕**：独立背景调查菜单改为商机数据源，新商机可直接调查；人工核验追加历史记录，客户编辑不再覆盖人工结论。
- [x] **3-5 Flowable 入驻审批闭环**：发起必须选择商机，流程 key、业务类型、businessKey 和商机变量一致；审批中/已通过禁止重复发起，驳回和撤回后允许重新发起，并使用数据库锁防止并发重复提交。
- [x] **3-6 审批回流与文件安全**：审批完成后客户、商机、审批状态和审批表回流；预览/下载校验流程实例确属当前商机，禁止套用其他流程审批人和意见。
- [x] **3-7 权限、日志、UI 与自动验证**：补齐商机、客户、标签、背景调查和入驻审核按钮权限；流程日志移除完整表单变量及企业敏感信息；入驻审核默认展示待处理并支持统计卡切换。
- [ ] **3-8 用户 UAT**：50 号 SQL 已在本地备份后执行；使用真实管理员、经理/招商员和普通园区账号验证权限、审批、驳回重提、历史留痕、回流及 Word 预览下载。

**代码复评分（UAT 前）**：

| 维度 | 分数 | 结论 |
| --- | ---: | --- |
| 安全 | 94 | 接口按钮权限、园区数据归属、流程实例绑定和发起权限已加固；待真实角色做越权反向复验 |
| 逻辑健壮性 | 93 | 企业并发去重、重复发起锁、驳回重提、状态回写失败回滚及审批回流异常提示已闭环 |
| 架构合规 | 90 | 背景调查和入驻审核拆分独立业务入口，园区校验集中复用，Controller/Service/Mapper 分层清晰 |
| BladeX 规范 | 89 | `@PreAuth`、统一返回、事务和 Mapper 参数绑定符合现有 BladeX 口径；SQL 50 已在本地执行，测试/生产库待部署执行 |
| 性能 | 82 | Flowable 主列表已改为服务端变量筛选；商机/客户列表标签填充仍有 N+1，登记为 P2 后续优化 |
| 代码质量 | 89 | 状态常量、错误提示和敏感日志已收口，自动检查通过；抄送列表仍受插件接口限制按标题筛选 |
| **综合** | **91** | 加权原始分 90.55，按整数记 91；达到代码门槛，P0/P1 代码缺陷为 0；本地 SQL 已完成，因真实角色 UAT、测试/生产 SQL 和流程/Word 证据未完成，暂不判定上线通过 |

**自动验证证据（2026-07-31）**：

- `cd Boot && mvn clean compile -DskipTests`：827 个源文件编译，`BUILD SUCCESS`。
- `cd saber3 && pnpm check:business-table-ui`：业务表格 UI 检查通过。
- 7 个本批 Vue SFC 文件语法解析通过；`git diff --check` 通过。
- `Boot/doc/sql/business/50_harden_settlement_management.mysql.sql`：2026-08-03 已在本地 `bladex_boot` 备份后执行；重复企业、背景调查缺园区和跨园区标签异常均为 `0`，字段、索引与保存权限校验通过。

**剩余 P2**：

- 商机列表和客户列表逐行填充标签存在 N+1 查询，后续数据量上升前应改为按主键批量查询标签关系。
- Flowable 抄送列表接口当前不支持表单变量筛选，入驻审核“抄送我”仍按标题关键字过滤，需在第 7 批协同办公统一优化。

**用户 UAT 清单**：

1. 普通园区账号只能看到本园区商机、客户、背景调查和标签，篡改其他园区 ID 或 `parkId` 应被拒绝。
2. 管理员可跨园区切换；编辑已有商机、客户或标签时不能通过请求参数迁移园区。
3. 按钮显示与接口权限一致：无新增、编辑、删除、核验、发起审核或审批表权限时，直接调用接口也应返回无权限。
4. 新增商机后立即出现在背景调查页面；连续保存两次人工核验后显示两条历史，核验人和核验时间正确。
5. 编辑已有客户后，人工核验状态、风险结论和历史记录保持不变。
6. 重复企业名称被拒绝；审批中商机不能编辑或删除；数据库并发冲突显示明确业务提示而非系统错误。
7. 经理账号可在首页/入驻审核看到真实 Flowable 待办并进入处理，待办数量与协同办公一致。
8. 被驳回或撤回的商机可重新发起；审批中和已通过商机不能重复发起；并发点击不会生成两个有效流程。
9. 审批完成后客户、商机、当前节点、审批状态、审批时间和审批文件地址正确回流。
10. 将其他流程实例 ID 传给当前商机的审批表预览/下载接口时应被拒绝。
11. 企业入驻审批表 Word 预览和下载文件字段一致，审批人、意见、流转记录与真实流程一致，文件名正常显示。

### 2.4 第 4 批：合同管理

**状态**：🧪 代码整改完成，待用户 UAT

审计合同列表、合同到期提醒、到期设置、合同归档、退租管理、合同打印模板和收款通知；重点验证合同生命周期、续租与变更、退租押金付款、收款确认、逾期前收款通知、附件与审批文件、园区数据隔离及表格统一规范。

**任务清单**：

- [x] **4-1 范围盘点**：完成合同主档、缴费计划、收付款确认、到期规则、归档、退租、打印模板、通知文件、Flowable 流程记录和菜单权限盘点。
- [x] **4-2 合同生命周期边界**：通用创建接口强制写入“待审批”，编辑接口仅接受业务白名单字段；合同状态、盖章文件、逻辑删除、园区归属和续租来源由专用业务动作维护。
- [x] **4-3 到期规则与园区隔离**：规则分页、保存、编辑、删除按当前账号所属园区校验楼宇；管理员保留跨园区能力；规则应用同时覆盖单楼宇和多楼宇合同。
- [x] **4-4 合同业务闭环**：合同创建、审批、盖章生效、续租、合同变更、归档、收款通知和缴费计划均有对应入口；合同变更登记即生效，不进入审批流程。
- [x] **4-5 退租与押金付款闭环**：退租审批、房屋验收、解除协议、交接资料完成后才允许生成押金付款账单并发起付款审批；审批完成后由财务在所有账单确认付款并回写退租状态。
- [x] **4-6 文件与附件闭环**：流程文件和退租资料区分展示；附件支持真实预览、下载、删除；合同审批表、退租审批表、解除协议、收付款及通知文件的生成入口和前置条件需以真实流程复验。
- [x] **4-7 权限、UI 与自动验证**：合同主列表、到期提醒/设置、归档详情和合同详情子表遵守边框、居中、操作单行、固定操作列口径；后端编译、表格检查和差异检查通过。
- [ ] **4-8 用户 UAT**：使用管理员、经理/招商员、财务和普通园区账号验证跨园区反向路径、真实多节点审批、续租状态回写、退租押金付款、收款确认、通知记录和 Word/Excel 预览下载。

**代码复评分（UAT 前）**：

| 维度 | 分数 | 结论 |
| --- | ---: | --- |
| 安全 | 93 | 合同和到期规则写入口收口到当前园区；通用编辑不能篡改生命周期、归档和园区字段；真实角色越权仍待 UAT |
| 逻辑健壮性 | 91 | 创建、编辑、续租、到期规则、多楼宇合同及付款园区无默认兜底已加固；退租与真实流程节点仍需复验 |
| 架构合规 | 88 | 园区访问复用 `ContractParkAccessService`，规则查询和合同更新通过 Mapper，业务逻辑保持 Service 层 |
| BladeX 规范 | 88 | `@PreAuth`、统一返回、事务、Mapper 参数绑定及前端表格规范检查通过 |
| 性能 | 82 | 到期规则和多楼宇匹配保留分页与批量更新；`FIND_IN_SET` 为历史逗号字段兼容方案，真实数据量下需记录耗时 |
| 代码质量 | 89 | 生命周期白名单和错误提示明确，重复的园区校验口径已收敛；暂无专门自动化业务测试 |
| **综合** | **89** | 加权原始分 89.35，按整数记 89；达到代码门槛，但因真实角色、流程和文件证据未完成，暂不判定上线通过 |

**自动验证证据（2026-07-31）**：

- `cd Boot && mvn clean compile -DskipTests`：`BUILD SUCCESS`。
- `cd saber3 && pnpm check:business-table-ui`：业务表格 UI 检查通过。
- `git diff --check`：通过。
- 本批未新增数据库字段或 SQL，不需要追加 `### 10.4 SQL 更新` 执行记录。

**用户 UAT 清单**：

1. 普通园区账号只能看到本园区合同、到期规则、归档和退租数据；篡改合同 ID、规则 ID、楼宇 ID 或 `parkId` 应被拒绝。
2. 管理员可跨园区查看和维护规则；普通账号未绑定园区时返回明确错误，不得默认落到园区 `1`。
3. 创建合同请求携带“生效”或盖章文件时，数据库仍为“待审批”，盖章生效只能通过专用入口完成。
4. 编辑合同请求携带其他园区、合同状态、盖章文件、删除标志或续租来源字段时，系统保留原值且只更新业务字段。
5. 创建包含多个楼宇的合同，修改到期规则后，单楼宇和多楼宇合同均能命中提醒；规则改为其他楼宇后旧合同提醒不应继续误命中。
6. 续租合同审批、盖章生效后新旧合同状态正确回写，合同编号、来源合同和缴费计划不串合同。
7. 退租必须按审批、验收、解除协议、交接资料、押金付款审批、财务确认付款顺序完成；未完成前置条件时接口应明确拒绝。
8. 收款账单确认收款要求收款时间和收款凭证；收款通知仍属于合同管理，逾期催缴记录不得混入付款账单。
9. 合同详情、归档详情、缴费记录、审批记录、附件和退租记录的表头、内容、操作列均居中且操作单行。
10. 使用真实样例复验合同审批表、合同正文、退租审批表、解除协议、交接验收单、收款通知和付款/开票文件的预览、下载、文件名、页数和审批回流。

### 2.5 第 5 批：逾期管理

审计所有账单、逾期账单、逾期提醒、逾期通知和生成文件；重点验证 direction 口径、确认收付款、催交通知接收人、补缴后历史留存、律师函申请及文件闭环。

本批代码整改结果：

1. 收付款账单、逾期查询和收款通知继续统一以账单 `direction` 区分应收与应付，逾期催缴只允许应收账单进入。
2. 逾期内部通知已按实际更新行数返回已读结果；无当前用户通知或跨园区账单不会被误报为操作成功。
3. 催缴接收人限定为当前租户、当前园区及其子部门的启用账号，避免同租户不同园区之间误发逾期信息。
4. 逾期处置抽屉的律师函、退租、房屋验收流程记录按当前账单精确关联；兼容旧退租/验收记录没有 `payment_id` 的历史资料，补缴后仍保留逾期历史入口。
5. Workflow 后端补充逾期律师函流程启动校验：必须关联应收账单，账单已真实逾期且未结清；付款账单、未来账单和已结清账单不能绕过前端直接发起。
6. 后端 `mvn clean compile -DskipTests`、前端 `pnpm check:business-table-ui` 和 `git diff --check` 均通过；本批无新增 SQL。

代码复评分：安全 93、逻辑健壮性 93、架构合规 86、BladeX 规范 86、性能 82、代码质量 86，综合 **89 分**。P0/P1 代码问题已清零；剩余为真实角色、跨园区、补缴后历史留存和 Flowable 节点的用户 UAT，未标记上线通过。

### 2.6 第 6 批：企业服务

审计物业服务配置、工单处理、商户管理、广告管理、服务管理、政策服务和在园企业数据；重点验证菜单层级、数据联动、园区政策可见性、接口真实性和统一表格 UI。

本批审计与修复结果：

1. 统一企业服务业务的园区数据访问口径，普通账号使用所属园区，缺少部门时直接拒绝访问，不再默认落到园区 1；详情、列表、统计、删除和写入均经过 `ParkDataAccessService`。
2. 新增服务项、商户、广告、政策服务必须带有效园区；广告关联商户、增值服务单关联商户必须与业务记录属于同一园区，服务单编辑不允许通过请求体迁移园区。
3. 物业工单、商户服务单、政策服务和广告的状态值由后端校验，非法状态不能通过直接构造请求体写入。
4. 在园企业数据看板继续按需求固定汇总全部园区，智能设备卡片改用明确的“全部园区设备统计”接口，避免与账单、合同、房源统计口径不一致；普通设备台账查询仍保持园区隔离。
5. 后端 `mvn clean compile -DskipTests`、前端 `pnpm check:business-table-ui` 和 `git diff --check` 均通过；本批无新增 SQL，未执行 SQL。

代码复评分：安全 92、逻辑健壮性 91、架构合规 88、BladeX 规范 88、性能 83、代码质量 87，综合 **89 分**。P0/P1 代码整改完成；菜单真实账号、小程序登录态、跨园区正反向路径和工单/服务单真实数据闭环仍需用户 UAT，暂不标记上线通过。

### 2.7 第 7 批：协同办公

审计 Flowable 组件的待办、已办、发起、抄送、流程设计、表单、权限和业务回流；插件内部 UI 不做无必要重写，重点保证身份映射、状态一致性和异常恢复。

### 2.8 第 8 批：首页、菜单与全局 UI

审计首页工作台、导航、菜单层级、标签栏、基础容器、卡片和全局表格规范；重点验证入口跳转不丢菜单、待办/提醒数字真实、布局遵守 `AGENTS.md` 和业务表格检查脚本通过。

### 2.9 第 9 批：流程、模板打印与通知专项

对照验收清单第 7、8、9 节，逐一验证流程节点、审批回流、全部模板字段/版式/预览/下载、站内信及外部通知；模板数量、业务名称、生成入口和前置条件形成唯一清单。

本轮重点审计“入口参数 → 流程表单 → Flowable 变量 → 业务流程记录 → 预览/下载文件”的数据回流链路，已完成以下代码整改：

1. 通用工作流入口统一读取业务页面传入的 `params`，无直达参数时回退读取流程列表返回的 `variables`，修复从流程入口发起时租客/企业名称及合同、账单字段为空的问题。
2. 通用发起页和外置表单统一使用 `js-base64` 编解码，覆盖中文企业名称、租客名称、房源名称和审批事项；同步修复发起、待办、已办、外置入驻详情等路由的解码方式，避免中文乱码或 Base64 解析失败。
3. 动态表单字段映射补齐“企业名称、租客名称、租户名称、承租方、客户名称、对方名称”等业务标签，并支持 `enterpriseName`、`customerName`、`tenantName`、`lesseeName` 等字段别名。
4. 已按 7 个正式审批入口建立字段白名单：入驻审批、合同审批、开票申请、付款申请、律师函申请、退租申请、房屋验收申请。所有入口统一传递业务主键、业务类型、企业/租客上下文和申请人信息；金额、账期只在开票、付款、律师函和退租等确有该字段的表单中传递，房屋验收不强制携带金额。
5. 退租审批入口在打开流程前按 `customerId` 读取客户档案，自动回填“退租申请单位（个人）”和“申请人联系方式”；流程参数同时传递客户名称、联系人、联系电话、通用别名及退租模板字段 ID，退租管理后续房屋验收、押金付款流程沿用同一客户数据。后端生成退租审批表时继续以流程表单值优先、客户档案联系电话兜底。
6. 合同、所有账单和逾期管理的流程入口统一补全合同与客户档案上下文，传递 `customerId`、企业/租客名称别名、联系人、联系电话、统一社会信用代码和注册地址，避免同一流程从不同页面发起时字段口径不一致。
7. 合同流程完成回调调整为先保存最终节点 `formDataJson` 快照，再在同一事务内生成审批文件，生成成功后回写文件地址；解决最终审批节点新增意见或字段尚未入库时，文书读取上一节点旧快照的问题。

#### 7 个审批入口回流矩阵

| 审批入口 | 权威业务主键 | 主要回流字段 | 审批完成输出 | 代码状态 |
| --- | --- | --- | --- | --- |
| 入驻审批 | `opportunityId` | 企业名称、负责人、联系方式、园区、经营范围、意向房源 | 企业入驻审批表 | 已补全，待真实经理节点复验 |
| 合同审批 | `contractId` | 租客、联系人、合同、房源、园区、租金与期限 | 合同会签审批表 | 已补全，待多节点复验 |
| 开票申请 | `paymentId` | 企业、联系方式、信用代码、注册地址、应收金额、账期 | 开票申请表 | 已补全，待财务流程复验 |
| 付款申请 | `paymentId` | 收款方、联系方式、应付金额、账期、付款方向 | 付款申请表 | 已补全，待财务流程复验 |
| 律师函申请 | `paymentId` | 租客、合同、逾期金额、逾期天数、账期 | 律师函及逾期文件 | 已补全，待法务流程复验 |
| 退租申请 | `contractId` | 退租单位、联系电话、退租类型、日期、原因及结算数据 | 退租审批表 | 已补全，待真实租客数据复验 |
| 房屋验收申请 | `contractId` | 租客、合同、房源、退租流程关联、验收数据 | 房屋验收表、交接单 | 已补全，不强制金额字段，待验收节点复验 |

当前仍需用户专项 UAT：按上表 7 个入口使用招商/经理、财务、部门领导、分管领导等最终节点账号逐一发起和审批；退租审批重点核对“退租申请单位（个人）”等于合同租客名称、“申请人联系方式”等于客户档案联系电话；同时核对首页待办、流程详情、业务流程记录、审批表预览、Word/Excel 下载文件中的租客/企业名称、审批人、意见、时间和业务主键一致。真实流程节点、模板版式、OSS 文件、通知发送结果未由本轮静态审计替代，暂不标记第 9 批正式上线通过。

代码复核分：安全 90、逻辑健壮性 91、架构合规 88、BladeX 规范 88、性能 84、代码质量 88，综合 **89 分**；前端 `pnpm check:business-table-ui`、中文 Base64 回环、后端编译和 `git diff --check` 均已通过。

### 2.10 第 10 批：安全、性能、部署与最终验收

对照验收清单第 10、11 节完成真实角色安全复验、性能基准、慢查询、稳定性、备份恢复、LibreOffice/字体、环境变量、部署回滚和全量回归，最终按最低模块分给出上线结论。

---

## 3. 决策点与风险登记

### 3.1 待决策事项

| 编号 | 决策点 | 建议方案 | 影响 | 状态 |
| --- | --- | --- | --- | --- |
| D-01 | 参数签名启用范围 | 对合同、财务/企业服务、审批、园区、Workflow 的写接口启用时间戳 + nonce 防重放签名；资源上传、下载、预览和回调不纳入首批 | 避免直接全局启用破坏直传文件与第三方回调 | ✅ 已按关键写接口分阶段启用 |
| D-02 | 历史泄露凭据轮换 | 代码改环境变量后立即轮换数据库、Redis、OSS、Docker 仓库密码 | 仅删除代码不能使已暴露密码失效 | ⏳ 待运维执行 |
| D-03 | P1 上线豁免机制 | 原则上不豁免；确需豁免须记录责任人、截止日期、补偿措施和批准人 | 防止未闭环问题被口头放行 | ✅ 采用严格书面豁免 |

### 3.2 全局风险

| 风险 | 影响 | 缓解措施 |
| --- | --- | --- |
| 测试只使用管理员账号 | 无法发现菜单、数据和园区越权 | 每批至少使用管理员、经理/业务员、财务和普通企业用户复验 |
| 历史脏数据影响结论 | 新数据通过但旧数据仍异常 | 每批建立新旧两组样本，区分代码缺陷与迁移数据问题 |
| 流程配置与代码脱节 | 开发环境通过、真实节点无待办 | 固化流程 key、版本、角色/用户映射和发布记录 |
| 模板/字体/LibreOffice 环境差异 | 预览与下载版式不一致 | 部署检查字体、soffice 路径、缓存和模板版本 |
| 批次范围持续扩张 | 无法形成可提交、可回滚的小版本 | 每批只修本批 P0/P1，新需求登记到对应批次 |

---

## 4. 用户 UAT 证据模板

每个二级菜单至少记录以下证据：

| 字段 | 记录要求 |
| --- | --- |
| 页面/接口 | 菜单路径、页面 URL、接口 URL |
| 账号与角色 | 不记录密码；记录角色、部门、园区、租户 |
| 样本数据 | 业务主键、企业/合同/账单等脱敏标识 |
| 正向路径 | 新增、查询、修改、审批、下载等预期结果 |
| 反向路径 | 越权、重复提交、空值、驳回、失败重试等结果 |
| 数据回流 | 页面、数据库、流程、通知、文件之间的一致性 |
| 性能 | 关键接口耗时、慢查询及数据量 |
| 结论 | 通过 / 不通过 / 待复验，附截图或日志时间 |

---

## 5. 完成度跟踪

| 批次 | 任务总数 | 已完成 | 完成度 | 状态 |
| --- | ---: | ---: | ---: | --- |
| 1 | 8 | 0 | 0% | ⏸️ |
| 2 | 8 | 7 | 88% | 🧪 |
| 3 | 8 | 7 | 88% | 🧪 |
| 4 | 8 | 7 | 88% | 🧪 |
| 5 | 8 | 7 | 88% | 🧪 |
| 6 | 8 | 7 | 88% | 🧪 |
| 7 | 待盘点 | 0 | — | ⏭️ |
| 8 | 8 | 7 | 88% | 🧪 |
| 9 | 8 | 7 | 88% | 🧪 |
| 10 | 待盘点 | 0 | 0% | ⏳ |

---

## 6. 提交与维护规则

1. 每批只形成一个用户提交点，不自动提交、不自动推送。
2. 用户提交后，将 commit hash 回填到 §0.2、§5 和 Changelog，再启动下一批。
3. 只允许将任务从 `- [ ]` 改为 `- [x]`，不得删除未完成任务掩盖范围。
4. 新发现的问题写入当前批风险或对应后续批次，不顺手跨批修改。
5. 路线图状态与 Git 不一致时，以 Git 事实核验后修正，禁止编造提交哈希。

---

## 7. Changelog

### 2026-08-03 · 第 9 批代码审计与整改完成并提交 / 待专项 UAT

- 第 9 批已完成流程入口参数、中文 Base64、动态表单字段映射、7 个审批入口业务上下文和最终节点表单快照的代码审计与整改。
- 后端编译、前端业务表格检查、Vue SFC/脚本解析和 `git diff --check` 均已通过；本批未新增 SQL。
- 本批提交为 `2799567`。当前状态由“🚧 进行中”修正为“🧪 代码审计完成、待真实多角色专项 UAT”。
- 未完成部分是招商/经理、财务、部门领导、分管领导等真实账号的多节点审批、模板预览下载和通知结果复验，不属于代码审计遗漏。

### 2026-07-31 · 第 8 批首页、菜单与全局 UI 代码整改完成 / 待用户 UAT

- 按用户要求跳过第 7 批协同办公，本轮进入第 8 批首页、菜单与全局 UI 审计。
- 统一左侧菜单宽度为 `260px`、主体内容页边距为 `22px`，修复顶部账号名称字段兼容，统一首页“逾期通知”文案。
- 修复首页入口 query 参数的标准解析，并让顶部菜单在无匹配业务路由时回到首页态；设置抽屉移除会破坏固定导航布局的“顶部菜单布局”切换入口。
- 后端 `mvn clean compile -DskipTests`、前端 `pnpm check:business-table-ui` 和 `git diff --check` 均通过；本批无新增 SQL，未执行 SQL。
- 日程正式接口、首页政策通知接口的独立数据源以及领导角色真实权限差异仍待用户 UAT，本批暂不标记上线通过、不自动提交。

代码复评分：安全 90、逻辑健壮性 90、架构合规 88、BladeX 规范 88、性能 84、代码质量 88，综合 **88 分**。

### 2026-07-31 · 第 9 批流程、模板打印与通知专项启动 / 代码整改完成

- 修复通用工作流入口只读取 `row.params` 导致流程列表发起时租客/企业名称不回填的问题，改为 `params` 优先、`variables` 回退。
- 统一工作流发起、详情及外置表单的中文 Base64 编解码，补齐企业名称、租客名称、对方名称等动态表单字段映射。
- 已完成入口与后端 Flowable 变量/业务主键/文件回流链路静态核对；真实多节点审批、模板版式、文件下载和通知发送仍待用户专项 UAT。
- 本批无新增 SQL，不执行数据库操作；不自动提交代码。

### 2026-07-31 · 第 5 批逾期管理代码整改完成 / 待用户 UAT

- 逾期管理代码复评为 89 分，修复无通知已读误报成功、逾期处置流程串账单、旧退租/验收资料在补缴后历史消失以及跨园区催缴接收人过滤缺失等问题。
- Workflow 逾期律师函流程增加后端启动校验，不能仅依赖前端按钮禁用来阻止未来账单、付款账单和已结清账单。
- 本批无新增 SQL；后端编译、前端表格规范检查和差异检查通过。
- 当前不标记上线通过、不自动提交，等待用户使用管理员、招商/经理、财务和普通园区账号完成 UAT 后提交。

### 2026-07-31 · 第 4 批合同管理代码整改完成 / 待用户 UAT

- 合同管理代码复评为 89 分，已修复合同创建接口信任前端状态、通用编辑可覆盖生命周期字段、到期规则跨园区读写和多楼宇合同提醒遗漏等问题。
- 到期规则改为按当前账号园区查询，并在保存、编辑、删除时验证楼宇归属；付款服务移除无园区账号默认园区 `1` 的兜底。
- 合同详情与归档详情子表继续统一边框、居中和操作列口径；后端编译、表格规范检查和差异检查通过。
- 本批无新增 SQL；剩余工作为真实角色、Flowable 多节点、续租/退租押金付款、收款确认和模板预览下载 UAT。
- 当前不标记上线通过、不自动提交，等待用户 UAT 后提交。

### 2026-07-31 · 第 3 批代码整改完成 / 待用户 UAT

- 入驻管理代码复评为 91 分，P0/P1 代码缺陷已整改，后端编译、表格规范、Vue SFC 语法和差异检查通过。
- 新增独立背景调查/入驻审核接口，完成园区隔离、按钮权限、企业并发去重、人工核验留痕、Flowable 防重复发起和审批表流程实例绑定。
- 新增 `50_harden_settlement_management.mysql.sql`；2026-08-03 已在本地 `bladex_boot` 备份后执行通过，目标测试/生产库执行前仍须重新检查重复企业、背景调查缺园区及跨园区标签数据。
- 剩余 P2 为商机/客户标签 N+1 和 Flowable 抄送列表标题筛选；真实角色、驳回重提、审批回流和 Word 文件由用户 UAT。
- 当前不标记上线通过、不自动提交。

### 2026-07-31 · 第 2 批代码整改完成 / 待用户 UAT

- 园区管理代码复评为 89 分，P0/P1 代码缺陷已整改，自动编译与表格规范检查通过。
- 新增 `49_add_room_extension_permissions.mysql.sql`；2026-08-03 已在本地 `bladex_boot` 执行通过，水电、车辆查询/新增/删除权限菜单和角色授权完整，测试/生产库待部署执行。
- 因真实角色跨园区、历史数据兼容、页面操作和接口耗时尚未由用户复验，当前不标记上线通过、不自动提交。

### 2026-07-31 · 调整执行顺序 / 第 2 批启动

- 用户要求暂缓第 1 批，优先开展业务级审计。
- 当前切换至第 2 批园区管理；第 1 批已有未提交改动保留，不并入本批提交范围。

### 2026-07-31 · 路线图初始化 / 第 1 批启动

- 基准提交：`71d1cf5`。
- 建立 10 批审计整改路线和 88 分上线硬门槛。
- 第 1 批进入“全局安全、权限、日志与流程基础”审计阶段。
- 本批完成后停留在“待用户提交”，不自动进入第 2 批。
