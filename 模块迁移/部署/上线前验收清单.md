# 上线前验收清单

> 适用范围：BladeX 园区管理项目正式上线前总验收。  
> 验收口径：所有 P0 必须通过，P1 原则上通过或有明确上线豁免记录，P2 可进入上线后迭代。

## 0. 上线结论

| 项目 | 结论 | 负责人 | 验收时间 | 备注 |
| --- | --- | --- | --- | --- |
| 是否允许上线 | 待确认 |  |  | P0 全部通过后填写 |
| 上线版本号 / Git Tag |  |  |  | 例：`release/park-202607xx` |
| 后端包版本 |  |  |  | jar 文件名、MD5 |
| 前端包版本 |  |  |  | dist 包名、MD5 |
| 数据库备份文件 |  |  |  | 备份路径、大小、时间 |
| 回滚方案是否演练 | 待确认 |  |  | 至少测试环境演练一次 |

## 1. 代码与版本收口

| 优先级 | 检查项 | 验收标准 | 结论 | 证据 / 备注 |
| --- | --- | --- | --- | --- |
| P0 | Git 工作区干净 | 除明确忽略文件外，无未提交代码 | 待验收 | `git status --short` |
| P0 | 测试产物排除 | `.playwright-cli/`、`output/` 等不进入提交和部署包 | 待验收 | `.gitignore` 或手工排除记录 |
| P0 | 所有上线代码已提交 | 后端、前端、SQL、部署文档都在同一提交范围内 | 待验收 | commit id |
| P0 | 创建上线 Tag | 可通过 Tag 精确还原上线版本 | 待验收 | tag 名称 |
| P1 | 代码审查完成 | 关键改动无 P0/P1 级问题 | 待验收 | review 记录 |
| P1 | 编译通过 | 后端 `mvn -DskipTests clean compile` 通过；前端生产构建在上线前通过 | 待验收 | 编译日志 |

## 2. SQL 与数据库变更

### 2.1 SQL 执行清单

#### 2.1.1 执行策略

当前测试环境已采用“本地完整库复制到测试库 `wzjk`”作为基线。上线前 SQL 策略按下面口径执行：

| 优先级 | 检查项 | 验收标准 | 结论 | 证据 / 备注 |
| --- | --- | --- | --- | --- |
| P0 | 基线库确认 | 测试环境 `wzjk` 已由完整可用库复制，不再重复执行早期全量迁移脚本 | 待验收 | 参考 `模块迁移/部署/测试环境部署说明.md` 第 3 节 |
| P0 | 生产执行策略确认 | 生产库不得直接覆盖整库；只能按已确认的增量 SQL 执行，或另行制定完整迁移方案 | 待验收 | 上线评审确认 |
| P0 | 执行前备份 | 执行任何生产 SQL 前，必须完成数据库备份并记录备份文件路径 | 待验收 | 备份文件、大小、时间 |
| P0 | 增量脚本白名单 | 只执行本清单“当前需执行增量 SQL”中的脚本 | 待验收 | 本节 2.1.2 |
| P0 | 执行顺序明确 | 先菜单清理 / 菜单命名，再首页迁移，再新增接口权限和业务字段 | 待验收 | 本节 2.1.2 |
| P0 | 执行命令明确 | 每个需要执行的增量 SQL 有服务器命令、Docker 命令或等价执行方式 | 待验收 | 本节 2.2 |
| P0 | 执行后验证 | 每个 SQL 执行后都有查询验证方式，验证结果写入 2.2 | 待验收 | 查询截图 / 查询结果 |
| P1 | 幂等性检查 | 重复执行不会产生重复菜单、重复角色授权、重复字段 | 待验收 | `COUNT(*)`、唯一 code / path 查询 |

#### 2.1.2 当前需执行增量 SQL

以下脚本是“完整库复制基线”之后登记过的增量 SQL。上线前以 2.2 的执行记录为准，生产执行前必须重新确认目标库是否已执行过。

| 顺序 | 优先级 | 改动内容 | SQL 文件 | 执行条件 | 验证方式 |
| --- | --- | --- | --- | --- | --- |
| 1 | P0 | 移除合同管理下的“合同创建模板”二级菜单 | `Boot/doc/sql/contract/12_remove_contract_create_template_menu.mysql.sql` | `blade_menu` 中仍存在 `contract_create_template` 或 `/contract/create-template` | 查询对应菜单数量，应为 `0` |
| 2 | P0 | 财务管理一级菜单改名为“逾期管理” | `Boot/doc/sql/finance/03_rename_finance_menu_to_overdue.mysql.sql` | 一级菜单 `/finance` 名称不是 `逾期管理` | 查询 `path='/finance'` 且 `parent_id=0` 的 `name`，应为 `逾期管理` |
| 3 | P0 | 首页迁移至原工作台菜单，工作台一级菜单改名为“首页” | `Boot/doc/sql/business/22_migrate_home_to_desk.mysql.sql` | 仍存在独立 `/home` 菜单，或 `desk` 未承载首页 | 查询 `code='desk'` 的 `name/component`；查询 `code='home'` 或 `path='/home'` 应为已删除 |
| 4 | P0 | 新增逾期提醒“联动日志”接口权限 | `Boot/doc/sql/business/12_finance_overdue_reminder_menu.mysql.sql` | 逾期提醒缺少联动日志按钮 / 接口权限 | 查询 `code='finance_overdue_reminder_log'` 应存在，角色授权数符合预期 |
| 5 | P0 | 清理 2.3 数据校验发现的历史脏数据 | `Boot/doc/sql/business/23_cleanup_2_3_validation_dirty_data.mysql.sql` | 存在园区旧 ID 断链、流程记录无效客户、运行中流程显示“流程结束”、已删除菜单仍有角色授权 | 2.3 断链与权限异常计数应为 `0` |
| 6 | P0 | 补齐账号锁定时长系统参数 | `Boot/doc/sql/business/26_add_account_lock_duration_param.mysql.sql` | `blade_param` 缺少 `account.lockDuration`，认证失败处理可能出现系统错误 | 查询 `account.failCount=5`、`account.lockDuration=30`，且均启用未删除 |
| 7 | P0 | 客户编辑抽屉补充租客类型、联系人与身份证附件字段 | `Boot/doc/sql/business/27_add_customer_edit_extra_fields.mysql.sql` | `biz_customer` 缺少客户编辑抽屉新增字段 | 查询 `tenant_type`、`approval_contact_name`、`bill_contact_name`、`contract_signer`、`identity_front_url`、`identity_back_url` 6 个字段，应全部存在 |
| 8 | P0 | 所有账单新增手工创建收款/付款账单、附件及选择房源字段 | `Boot/doc/sql/finance/04_add_manual_bill_fields.mysql.sql` | `biz_contract_payment` 缺少 `direction` 等手工账单字段 | 查询 12 个新增字段与 `idx_payment_direction` 索引应存在；`deposit_refund` 账单方向应为 `payable` |
| 9 | P0 | 修复历史开票流程记录打印地址误指向付款通知单 | `Boot/doc/sql/business/28_fix_invoice_workflow_print_file_url.mysql.sql` | 开票流程记录 `print_file_url` 不是 `/blade-contract/print/invoice-apply/{paymentId}` | 查询开票流程错误打印地址数量，应为 `0` |
| 10 | P1 | 优化楼层列表与租控看板房源查询索引 | `Boot/doc/sql/business/29_optimize_floor_room_query_indexes.mysql.sql` | `ics_room` 缺少园区或楼层组合索引 | 查询 `idx_room_park(park_id)`、`idx_room_building_floor(building_id,floor)` 均存在 |

#### 2.1.3 执行前禁止项

| 优先级 | 禁止项 | 原因 | 结论 |
| --- | --- | --- | --- |
| P0 | 禁止直接执行 `Boot/doc/sql/business/bladex_park_business.mysql.all.sql` | 聚合脚本不是当前可靠上线入口，可能遗漏或重复 | 待确认 |
| P0 | 禁止把 `ry_ics` 替换成 `wzjk` 后直接执行历史迁移脚本 | `ry_ics` 是历史来源库，不是运行库 | 待确认 |
| P0 | 禁止在生产库未备份时执行任何 DDL / 菜单清理 SQL | 无备份无法回滚 | 待确认 |
| P0 | 禁止重复执行会删除菜单或移动角色授权的 SQL | 可能影响生产菜单权限 | 待确认 |

### 2.2 SQL 执行记录

| 日期 | 改动内容 | SQL 文件 | 服务器执行命令 | 验证方式 | 执行结果 |
| --- | --- | --- | --- | --- | --- |
| 2026-07-06 | 补齐账号锁定时长系统参数 | `Boot/doc/sql/business/26_add_account_lock_duration_param.mysql.sql` | `mysql -h127.0.0.1 -P3306 -uroot -p wzjk < Boot/doc/sql/business/26_add_account_lock_duration_param.mysql.sql` | 查询 `blade_param` 中 `account.failCount`、`account.lockDuration` 的值，应分别为 `5`、`30` | 本地库 `bladex_boot` 已执行通过；测试/生产按目标库执行前需确认是否已存在 |
| 2026-07-07 | 客户编辑抽屉补充租客类型、联系人与身份证附件字段 | `Boot/doc/sql/business/27_add_customer_edit_extra_fields.mysql.sql` | `mysql --default-character-set=utf8mb4 -h127.0.0.1 -P3306 -uroot -p wzjk < Boot/doc/sql/business/27_add_customer_edit_extra_fields.mysql.sql` | 查询 `biz_customer` 中 `tenant_type`、`approval_contact_name`、`bill_contact_name`、`contract_signer`、`identity_front_url`、`identity_back_url` 6 个字段，应全部存在 | 测试库 `wzjk` 已执行通过；本地库 `bladex_boot` 执行前已存在字段 |
| 2026-07-14 | 所有账单新增手工创建收款/付款账单、附件及选择房源字段 | `Boot/doc/sql/finance/04_add_manual_bill_fields.mysql.sql` | `mysql --default-character-set=utf8mb4 -h127.0.0.1 -P3306 -uroot -p wzjk < Boot/doc/sql/finance/04_add_manual_bill_fields.mysql.sql` | 查询 `biz_contract_payment` 的 12 个新增字段和 `idx_payment_direction` 索引；抽样检查账单方向 | 本地库 `bladex_boot` 已执行通过；测试/生产库待部署时执行 |
| 2026-07-15 | 修复历史开票流程记录打印地址误指向付款通知单 | `Boot/doc/sql/business/28_fix_invoice_workflow_print_file_url.mysql.sql` | `mysql --default-character-set=utf8mb4 -h127.0.0.1 -P3306 -uroot -p wzjk < Boot/doc/sql/business/28_fix_invoice_workflow_print_file_url.mysql.sql` | 查询开票流程错误打印地址数量，应为 `0`；抽样 `record_id=10` 应为 `/blade-contract/print/invoice-apply/7` | 本地库 `bladex_boot` 已执行通过；异常计数 `0` |
| 2026-07-15 | 优化楼层列表与租控看板房源查询索引 | `Boot/doc/sql/business/29_optimize_floor_room_query_indexes.mysql.sql` | `mysql --default-character-set=utf8mb4 -h127.0.0.1 -P3306 -uroot -p wzjk < Boot/doc/sql/business/29_optimize_floor_room_query_indexes.mysql.sql` | 查询 `ics_room` 应存在 `idx_room_park(park_id)`、`idx_room_building_floor(building_id,floor)` 两个索引 | 本地库 `bladex_boot` 已执行通过，重复执行通过；两个索引及列顺序正确 |

### 2.3 数据校验

| 优先级 | 检查项 | 验收标准 | 结论 | 证据 / 备注 |
| --- | --- | --- | --- | --- |
| P0 | 核心表数量校验 | 园区、楼栋、楼层、房间、客户、商机、合同、账单数量符合预期 | 本地通过 / 待业务确认 | 2026-07-06 本地库 `bladex_boot`：园区 2、楼栋 2、楼层 38、房间 1、客户 1、商机 1、合同 6、账单 33、合同流程记录 16 |
| P0 | 外键 / 逻辑关联校验 | 合同关联企业、房源、账单、流程记录无断链 | 本地通过 | 执行 `23_cleanup_2_3_validation_dirty_data.mysql.sql` 后，园区、客户、商机、合同、账单、流程记录断链异常均为 `0` |
| P0 | 菜单权限校验 | 顶部菜单、左侧菜单、按钮权限与角色一致 | 本地通过 | 执行首页迁移 SQL 后，`desk` 为首页且 `/home` 已停用；已删除菜单角色授权清理后异常为 `0` |
| P1 | 历史数据兼容 | 老合同、老账单、老入驻数据能打开详情和导出 | 本地通过 | 登录本地后端抽样：合同 17/18 详情、账单 6/8 详情、商机 6 详情、归档审批表 17/18 导出、入驻审批表 6 导出均成功 |

#### 2026-07-06 2.3 数据校验记录

本地库：`127.0.0.1:3306/bladex_boot`。本次先按本地库清理和复验，执行前已备份相关表到 `/private/tmp/bladex_boot_2_3_cleanup_20260706_111021.sql`。

核心数量：

| 表 | 总数 | 有效 / 正常数 |
| --- | ---: | ---: |
| `ics_park` | 2 | 2 |
| `ics_building` | 2 | 2 |
| `ics_floor` | 38 | 38 |
| `ics_room` | 1 | 1 |
| `biz_customer` | 1 | 1 |
| `biz_business_opportunity` | 1 | 1 |
| `biz_contract` | 6 | 6 |
| `biz_contract_payment` | 33 | 33 |
| `biz_contract_workflow_record` | 16 | 16 |
| `biz_payment_notice` | 0 | 0 |

清理前异常：

| 类型 | 数量 | 明细 | 影响 |
| --- | ---: | --- | --- |
| 商机园区断链 | 1 | `biz_business_opportunity.opportunity_id=6` 使用历史园区 ID | 入驻详情、审批表园区字段可能回填不准 |
| 合同流程客户断链 | 1 | 合同流程记录存在无效客户 ID | 合同会签审批表、归档审批表的客户回流可能异常 |
| 已删除菜单残留授权 | 18 | `blade_role_menu` 仍绑定已删除菜单 | 权限数据有历史残留 |

清理后异常计数：

| 检查项 | 异常数 |
| --- | ---: |
| `customer_missing_park` | 0 |
| `opportunity_missing_park` | 0 |
| `opportunity_missing_customer` | 0 |
| `contract_missing_customer` | 0 |
| `contract_missing_room_id` | 0 |
| `contract_missing_building` | 0 |
| `contract_missing_park` | 0 |
| `payment_missing_contract` | 0 |
| `workflow_contract_missing_contract` | 0 |
| `workflow_payment_missing_payment` | 0 |
| `workflow_customer_missing_customer` | 0 |
| `role_menu_missing_menu` | 0 |
| `role_menu_deleted_menu` | 0 |

菜单 / 权限结果：

| 项 | 结果 |
| --- | --- |
| 顶部菜单 | 活跃入口包含：首页 `/desk`、园区管理 `/park`、入驻管理 `/settlement`、合同管理 `/contract`、逾期管理 `/finance`、企业服务 `/enterprise`、协同办公 `/plugin/workflow` |
| 首页迁移 | `desk` 活跃且名称为首页；`/home` 无活跃菜单 |
| 合同模板菜单 | `contract_create_template`、`/contract/create-template` 无活跃菜单 |
| 逾期联动日志权限 | `finance_overdue_reminder_log` 存在，授权 4 个角色 |
| 活跃业务菜单授权 | 未发现无角色授权的活跃业务菜单 / 按钮 |
| 历史脏授权 | 已清理，`blade_role_menu` 指向已删除菜单数量为 `0` |

接口抽样：

| 接口 | 结果 |
| --- | --- |
| `/blade-contract/contract/detail?contractId=17` | 通过，手填企业合同详情正常 |
| `/blade-contract/contract/detail?contractId=18` | 通过，客户关联合同详情正常 |
| `/blade-ics/payment/detail?paymentId=6` | 通过 |
| `/blade-ics/payment/detail?paymentId=8` | 通过 |
| `/blade-park/opportunity/detail?opportunityId=6` | 通过 |
| `/blade-contract/archive/detail/18` | 通过 |
| `/blade-contract/archive/export-approval/18` | 通过，返回合同会签审批表 docx 下载地址 |
| `/blade-contract/archive/export-approval/17` | 通过，手填企业合同也可导出合同会签审批表 |
| `/blade-contract/print/contract-approval/17`、`/blade-contract/print/contract-approval/18` | 通过，HTTP 200，文件头 `504b0304`，确认为 docx |
| `/blade-park/opportunity/tenant-entry/approval-form/6` | 通过，返回企业入驻审批表 |

## 3. 环境与生产配置

| 优先级 | 检查项 | 验收标准 | 结论 | 证据 / 备注 |
| --- | --- | --- | --- | --- |
| P0 | JDK 版本 | 生产环境 Java 17 可用 | 本地通过 / 生产待确认 | 本地 OpenJDK 17.0.15，Maven 3.9.11 使用 Java 17；生产服务器需再执行 `java -version` |
| P0 | MySQL 配置 | 数据库地址、账号、权限、字符集、时区正确 | 部分通过 / 生产待整改 | 本地 `bladex_boot` 连通，字符集 `utf8mb4`；测试库端口连通；`application-prod.yml` 仍为模板内网地址和固定账号，生产需替换 |
| P0 | Redis 配置 | Redis 设置密码，不暴露公网 | 部分通过 / 生产待整改 | 本地 Redis、测试 Redis 均 `PONG`；本地 dev 无密码仅限开发；`application-prod.yml` 使用固定默认密码，生产需替换并限制访问 |
| P0 | OSS / 文件存储 | Word、Excel、PDF、附件上传下载正常 | 不通过 / 待整改 | 本地 Word docx 下载通过；但 dev MinIO 指向本地 9000，当前端口未启动；prod/test OSS endpoint 未在目标网络复验 |
| P0 | 生产配置文件 | `prod` 配置不使用本地、测试库、默认密码 | 不通过 | `application-prod.yml` 存在固定 DB/Redis/OSS 配置、默认凭据和 localhost 域名；`saber3/src/config/website.js` 仍含 localhost 地址 |
| P0 | Token / 加密密钥 | 生产密钥已替换，长度和安全性满足要求 | 不通过 | 当前仍使用代码内静态 OAuth2 SM2、token sign-key、crypto-key；上线前必须替换并避免明文提交 |
| P0 | Nginx / 反向代理 | 前端路由刷新正常，`/api` 代理正确 | 部分通过 / 生产待配置 | 本地 Vite `/api` 代理和 history fallback 通过；仓库未发现生产 Nginx conf，生产需补 `try_files` 和 `/api` 反代 |
| P0 | HTTPS / 域名 | 正式域名 HTTPS 正常，证书未过期 | 待生产确认 | 当前未提供正式域名和证书，无法本地验收 |
| P1 | 日志目录 | 日志路径存在、权限正确、可滚动 | 待整改 | `logback-prod.xml` 存在滚动日志配置，但默认输出到相对路径 `target/blade/log`；生产建议改为 `/data/bladex-park/logs` 并确认权限 |
| P1 | 上传目录备份策略 | 附件和生成文档有备份或对象存储策略 | 待整改 | 本地 `Boot/uploadPath` 存在；生产附件、OSS bucket、生成文件需制定备份策略和恢复验证 |

#### 2026-07-06 第 3 节环境验收记录

本地已验证：

| 项 | 结果 |
| --- | --- |
| Java / Maven | OpenJDK 17.0.15；Maven 3.9.11 |
| 本地 MySQL | `127.0.0.1:3306/bladex_boot` 连通；MySQL 9.3.0；库字符集 `utf8mb4` |
| 本地 Redis | `127.0.0.1:6379` 连通，`PING=PONG` |
| 测试 DB / Redis 连通 | `192.168.19.16:3306`、`192.168.19.16:6379` 连通；测试 Redis `PING=PONG` |
| 本地后端 / 前端 | `127.0.0.1:8080`、`127.0.0.1:2888` 均在运行 |
| 前端代理 | `127.0.0.1:2888/api/blade-auth/oauth/captcha` 返回 200；`/contract/contract` history fallback 返回 200 |
| 文件下载 | 第 2.3 抽样中合同会签审批表下载返回 docx 文件流 |

待整改 / 待确认：

| 项 | 问题 | 处理要求 |
| --- | --- | --- |
| 生产 DB / Redis / OSS | `application-prod.yml` 仍是模板式固定配置 | 改为正式生产地址和强密码，最好使用环境变量或外置配置 |
| 生产域名 | 后端 `social.domain` 和前端 `website.js` 仍含 localhost | 替换为正式 HTTPS 域名，并确认回调、报表、流程设计器地址 |
| 加密密钥 | OAuth2 SM2、token、crypto key 仍为代码内静态值 | 生产生成新密钥，并同步前后端 SM2 公钥 |
| MinIO / OSS | 本地 9000 未启动；生产 endpoint 未复验 | 启动或配置可用 OSS，完成上传、预览、下载、备份测试 |
| Nginx | 仓库未发现生产 Nginx 配置 | 补 `try_files $uri $uri/ /index.html` 和 `/api` 反代配置 |
| HTTPS | 未提供正式域名和证书 | 部署后验证证书链、到期时间、HTTP 跳转 HTTPS |
| 日志目录 | prod 日志默认相对路径 | 上线前改成服务器固定目录并确认权限、滚动、磁盘空间 |
| 附件备份 | 未见明确备份策略 | 制定 OSS bucket / 上传目录备份和恢复演练 |

## 4. 登录、角色、菜单与权限

| 优先级 | 检查项 | 验收标准 | 结论 | 证据 / 备注 |
| --- | --- | --- | --- | --- |
| P0 | 管理员登录 | 管理员可登录、刷新 token 正常 | 本地通过 | `password` 登录、`/blade-auth/oauth/user-info`、`refresh_token` 本地接口均返回 200；不在文档记录口令 |
| P0 | 普通业务账号登录 | 招商、运营、财务、法务、领导账号可登录 | 待补验 | 本地有效账号仅 `admin`、`manager`、`boss`；未提供招商 / 运营 / 财务 / 法务 / 领导账号口令，未猜测密码避免锁号 |
| P0 | 顶部菜单 | 首页、园区管理、入驻管理、合同管理、企业服务、逾期管理、协同办公显示正确 | 本地通过 | 首页为前端固定顶部项；`/blade-system/menu/top-menu` 返回 6 个配置菜单：园区、入驻、合同、逾期、企业、协同 |
| P0 | 左侧菜单 | 点击顶部一级菜单后，左侧二级菜单正确，不出现 `none menu list` | 本地通过 | 6 个业务顶部菜单只显示当前一级菜单下二级菜单；首页显示除首页外的完整菜单树，并可通过顶部折叠按钮收起 / 展开；未出现 `none menu list` |
| P0 | 按钮权限 | 新增、编辑、删除、导入、导出、审批、打印等按钮按角色显示 | 部分通过 / 待角色补验 | `/blade-system/menu/buttons` 返回 389 个按钮；合同新增、档案导出审批表、逾期联动日志、收款通知下载、客户导入、园区新增等关键按钮均存在；仍需用普通角色截图确认差异显示 |
| P0 | 接口权限 | 无权限用户访问敏感接口被拒绝 | 部分通过 / 待角色补验 | 未登录访问 `/blade-system/menu/buttons`、`/blade-contract/contract/detail?contractId=18` 均返回 401；admin 访问合同详情成功；`blade_scope_api` 当前无业务动态接口权限配置，仍需普通无权限账号补验 403/拒绝场景 |
| P1 | 数据权限 | 非管理员只能看到授权范围内园区、合同、账单、日志 | 待补验 / 待配置 | admin 会绕过数据权限；本地 `blade_scope_data` 仅 1 条历史/示例配置，`blade_role_scope` 仅 1 条绑定；需补业务角色数据权限后用普通账号抽样验证 |

#### 2026-07-06 第 4 节本地验证记录

本次在本地环境验证登录、菜单与权限链路，未执行前端打包。

接口结果：

| 验证项 | 结果 |
| --- | --- |
| 管理员登录 | `/blade-auth/oauth/token` 返回 200，拿到 access token |
| 用户信息 | `/blade-auth/oauth/user-info` 返回 200，账号为 `admin` |
| 刷新 token | `grant_type=refresh_token` 返回 200，拿到新 access token |
| 顶部菜单接口 | `/blade-system/menu/top-menu` 返回 6 个配置顶部菜单；首页由前端固定项提供 |
| 全量路由 | `/blade-system/menu/routes` 返回 16 个一级路由，包含 `/desk` 首页 |
| 顶部菜单路由 | 6 个业务顶部菜单调用 `/blade-system/menu/routes?topMenuId=...` 均返回 1 个对应一级路由；首页调用全量路由显示完整左侧菜单树 |
| 按钮权限接口 | `/blade-system/menu/buttons` 返回 389 个按钮，关键业务按钮抽样 6/6 存在 |
| 未登录拦截 | 未登录访问菜单按钮、合同详情均返回 401 |
| 动态接口权限 | `blade_scope_api` 为 0 条，需后续补业务动态接口权限或明确仅采用菜单/注解权限 |
| 动态数据权限 | `blade_scope_data` 1 条、`blade_role_scope` 1 条；业务数据隔离仍待普通账号补验 |

Playwright UI 结果：

| 顶部菜单 | 点击后路由 | 左侧菜单结果 |
| --- | --- | --- |
| 园区管理 | `/park/rent-control` | 园区管理、租控管理、园区档案、建筑管理、楼层管理 |
| 入驻管理 | `/settlement/opportunity` | 入驻管理、商机管理、入驻审核、客户管理、客户标签 |
| 合同管理 | `/contract/contract` | 合同管理、合同列表、合同到期提醒、合同打印模板、合同归档、退租管理 |
| 逾期管理 | `/finance/bills-all` | 逾期管理、所有账单、逾期账单、收款通知、逾期提醒 |
| 企业服务 | `/enterprise/property-service-config` | 企业服务、物业服务、物业服务配置、工单处理、商户管理、政策服务、在园企业数据 |
| 协同办公 | `/plugin/workflow/pages/design/model` | 协同办公、流程设计、模型设计、表单设计、部署管理、流程按钮等 |
| 首页 | `/desk` | 回到首页后显示除首页外的完整菜单树，并支持顶部折叠按钮收起 / 展开左侧栏；未出现 `none menu list` |

本次修复：

| 文件 | 内容 |
| --- | --- |
| `saber3/src/store/modules/user.js` | `GetMenu(topMenuId)` 保留全量路由给动态路由，同时把左侧菜单提交为当前顶部菜单对应 routes，避免顶部点击后左侧仍为全量菜单 |
| `saber3/src/page/index/top/top-menu.vue` | 点击顶部“首页”时刷新默认菜单并跳转 `/desk`，避免保留上一个一级菜单 |

## 5. 首页工作台验收

| 优先级 | 检查项 | 验收标准 | 结论 | 证据 / 备注 |
| --- | --- | --- | --- | --- |
| P0 | 首页打开 | 登录后首页加载正常，无空白、无接口错误 | 本地通过 | Playwright 登录 admin 后 `/desk` 正常，控制台无 error |
| P0 | 指标卡片 | 数据有来源，空值有兜底 | 本地通过 | `/blade-home/home/workbench` 返回 200，指标有值且空值兜底为 0 |
| P0 | 审批待处理跳转 | 跳转到协同办公 / 我的事务 / 待办事宜 | 本地通过 | `/plugin/workflow/pages/process/todo`，左侧同步到协同办公 |
| P0 | 招商闭环入口 | 跳转到对应业务页面，左侧菜单同步正确 | 本地通过 | 抽样 6 个首页入口，路由和左侧一级菜单均正确 |
| P1 | 日历 / 待办 | 日程、待办展示正常，空状态合理 | 部分通过 | 日历和待办空状态正常；日程/政策正式接口仍在 `missingApis` |
| P1 | 不同角色首页 | 管理员、招商员、财务、领导看到的数据符合权限 | 部分通过 | admin、招商、财务账号接口 200；领导角色和数据权限差异待流程/角色配置完善后补验 |

#### 2026-07-06 第 5 节首页工作台验收记录

本地环境：前端 `127.0.0.1:2888`，后端 `127.0.0.1:8080`，数据库 `bladex_boot`。本次未执行前端打包。

| 检查项 | 结论 | 证据 / 备注 |
| --- | --- | --- |
| 首页打开 | 本地通过 | Playwright 登录 admin 后进入 `/desk`，页面标题为“首页”，控制台无 error；截图 `.playwright-cli/page-2026-07-06T06-28-13-635Z.png` |
| 指标卡片 | 本地通过 | `/blade-home/home/workbench` 返回 200；本地数据：楼层/房间 1、客户 1、即将到期合同 1、审批待处理 0、待办任务 0；空值统一显示 0 |
| 审批待处理跳转 | 本地通过 | 首页“审批待处理”点击后进入 `/plugin/workflow/pages/process/todo`，页面标题“待办事宜”，左侧菜单为“协同办公 / 我的事务 / 待办事宜”；截图 `.playwright-cli/page-2026-07-06T06-29-07-273Z.png` |
| 招商闭环入口 | 本地通过 | 抽样 6 个入口均通过：合同到期 `/contract/expiring`->合同管理，租金收缴 `/finance/bills-all`->逾期管理，入驻审核 `/settlement/project-approval`->入驻管理，物业工单 `/enterprise/property-workorder`->企业服务，楼层管理 `/park/floor`->园区管理，我的审批 `/plugin/workflow/pages/process/my-done`->协同办公 |
| 日历 / 待办 | 部分通过 | 日历按当前月份展示，待办卡片空状态正常；日程、政策通知正式接口仍在 `missingApis` 中，本期为页面占位 |
| 不同角色首页 | 部分通过 | admin、招商部用户、财务部用户访问 `/blade-home/home/workbench` 均返回 200；领导角色账号和数据权限差异仍需在流程节点/角色配置完善后补截图 |

本次发现并修复：

| 文件 / SQL | 修复内容 |
| --- | --- |
| `Boot/src/main/java/org/springblade/modules/home/controller/HomeController.java` | 首页接口权限由旧 `home` 菜单改为当前 `desk` 菜单，避免普通用户访问 `/blade-home/home/workbench` 返回 401 |
| `Boot/src/main/java/org/springblade/modules/system/service/impl/ParamServiceImpl.java` | 参数缺失时返回 `null`，让调用方默认值生效，避免 `account.lockDuration` 缺失时认证失败处理抛系统错误 |
| `Boot/doc/sql/business/26_add_account_lock_duration_param.mysql.sql` | 增加幂等 SQL，补齐 `account.lockDuration=30` |
| `Boot/doc/sql/business/27_add_customer_edit_extra_fields.mysql.sql` | 客户编辑抽屉补充租客类型、审批联系人、账单联系人、合同签署人、身份证正反面字段 |
| `saber3/src/views/desk.vue` | 首页内部入口跳转前同步顶部菜单对应的左侧菜单，避免从首页进入合同/逾期/协同办公后左侧仍显示全量菜单 |

接口结果：

| 账号 | `/blade-home/home/workbench` | 顶部菜单 | `/desk` 路由 |
| --- | --- | --- | --- |
| admin | 200 | 6 个配置顶部菜单 | 存在 |
| 招商部用户 | 200 | 6 个配置顶部菜单 | 存在 |
| 财务部用户 | 200 | 6 个配置顶部菜单 | 存在 |

## 6. 业务模块验收

### 6.1 园区管理

| 优先级 | 检查项 | 验收标准 | 结论 | 证据 / 备注 |
| --- | --- | --- | --- | --- |
| P0 | 园区档案 | 列表、详情、新增、编辑、删除正常 | 本地通过 | 2026-07-06 本地 18080 最新代码：`/blade-park/park/submit` 新增、编辑，`/detail`、`/list`、`/remove` 均通过；中文 JSON 已验证正常落库，示例 `parkId=27`，中文 HEX 为 `E9AA8CE694B6E59BADE58CBAE7BC96E8BE91...`；临时数据已清理为 `0` |
| P0 | 楼栋管理 | 楼栋与园区关联正确 | 本地通过 | 新增 `buildingId=14`，`/blade-park/building/detail` 返回 `parkId=27`、`parkName=验收园区编辑-1783327588`、`floors=3`；删除前已验证楼栋归属园区正确 |
| P0 | 楼层管理 | 新增房间抽屉与租控管理一致 | 本地通过 | 后端：楼栋提交后自动同步 3 个楼层，二层 `floor2Id=91` 编辑面积为 `222.22` 后 `/floor/detail` 正常；前端：`rent-control.vue` 新增房间抽屉默认建筑/楼层、建筑选择值、备注字数限制已与租控抽屉一致，SFC 解析/编译通过 |
| P0 | 房态管理 | 房间状态、面积、租控状态准确 | 本地通过 | 新增 `roomId=16`，详情返回 `area=88.88`、`status=0`；调用 `/blade-park/room/change-status?id=16&status=2` 后详情返回 `status=2`，租控看板统计 `reservedRooms=1` |
| P1 | 数据联动 | 园区、楼栋、楼层、房间筛选联动正确 | 本地通过 | `/blade-park/rent-control/board?parkId=27&buildingId=14&floorNo=2&status=2` 返回当前园区/楼栋一致，二层 `totalArea=222.22`、`usedArea=88.88`，房间 `验收房间-1783327588` 出现在对应楼层；验收临时园区、楼栋、房间清理校验 `0 0 0` |

### 6.2 入驻管理

| 优先级 | 检查项 | 验收标准 | 结论 | 证据 / 备注 |
| --- | --- | --- | --- | --- |
| P0 | 商机管理 | 新增、编辑、跟进、转换客户正常 | 本地通过 | 本地 18080：新增商机 `opportunityId=8`，编辑意向面积 `99.99` 成功；跟进列表返回 2 条记录；提交入驻审批并审批通过后生成客户 `customerId=10`，验收临时数据 `ACCEPT62-*` 已清理 |
| P0 | 入驻审批发起 | 可选择真实流程并发起成功 | 本地通过 | 本地补齐并使用已发布 `tenant_entry` 流程 `flowId=5`（企业入驻审批，2 个节点）；商机提交审核生成审批项目 `projectId=21`，状态为审批中 |
| P0 | 入驻审批状态 | 审批中、已完成、被驳回、已撤回只显示真实状态 | 本地通过 | 以 `processStatus=1/2/3/4` 分别筛选本次验收项目，返回状态分别仅为 `1/2/3/4`；样例项目 `22/21/23/24` 已随验收数据清理 |
| P0 | 入驻审批表导出 | 审批人、意见、流转信息回流到表单 | 本地通过 | `/blade-approval/project/form/export/21` 返回 `templateName=企业入驻审批表`，日志 3 条：经办人提交、部门审批同意、分管领导审批同意；发现并修复同一审批人连续节点无法推进的问题 |
| P1 | 客户回流 | 入驻审批通过后客户数据生成 / 更新正确 | 本地通过 | `projectId=21` 审批通过后，客户 `customerId=10` 自动生成，`settlementStatus=3`；验收临时商机、审批项目、日志、客户清理校验均为 `0` |

### 6.3 合同管理

| 优先级 | 检查项 | 验收标准 | 结论 | 证据 / 备注 |
| --- | --- | --- | --- | --- |
| P0 | 合同创建 | 入驻企业和手填企业均可创建合同 | 本地通过 | 本地 8080/18080：手填企业 `contractId=26`、入驻企业 `contractId=27` 均可查询；手填企业 `customerId` 为空、企业名正常回填，入驻企业关联客户 `customerId=9` |
| P0 | 房源与价格 | 创建合同时房源、价格、政策、自定义条款正确写入 | 本地通过 | `contractId=26`：龙西大厦 1701，面积 111，单价 39，月租 4329，物业 5，押金 4329，自定义条款在备注；`contractId=27`：单价 41，月租 4551，物业 6，押金 9102，浮动租金 `2027-03-01起租金递增5%` |
| P0 | 合同审批 | 发起、审批、驳回、通过正常 | 本地部分通过 | `contractId=26/27` 均生成 `contract_approval` 流程记录并自动通过，状态回写到待盖章/生效；当前 `contract_sign` 流程只有 1 个用户节点且 `skipFirstNode=true`，发起后无待办任务，人工审批、驳回需配置真实节点后补验 |
| P0 | 合同下载 | 审批盖章前可下载合同初稿 | 本地通过 | `contract-text/fixed/26`、`contract-text/floating/27` 均返回 Word OOXML，HTTP 200，文件头 `504b0304` |
| P0 | 合同归档 | 查看档案可导出合同会签审批表 | 本地通过 | 档案详情抽屉有“导出合同审批表”和“下载合同正文”；`contract-approval/26`、`contract-approval/27` 均返回 Word OOXML，HTTP 200 |
| P0 | 合同状态 | 待审批、待盖章、生效、退租中、已退租等状态准确 | 本地部分通过 | 本地接口已验证 `0=待审批`、`1=生效`、`4=已退租`、`5=待盖章`；退租管理补验有 1 条数据：`ACCEPT-20260702-CN-165426`，页面显示“已通过 / 退租已完成 / 已退租”，房屋验收记录 `recordId=20`、押金退还 `paymentId=14` 已完成；按 2026-07-07 确认口径，房屋验收线下完成，不强制线上审批中间态，系统补充“登记验收情况 / 上传支付凭证”入口，审批流配置暂不调整；截图：`output/playwright/6-3-contract-termination.png` |
| P1 | 操作入口 | 不需要的重复导出按钮已隐藏 | 本地通过 | 合同列表操作仅显示“详情 / 归档”；下载合同在详情抽屉，合同会签审批表导出在查看档案抽屉；截图：`output/playwright/6-3-contract-list-detail.png`、`output/playwright/6-3-contract-archive-drawer.png` |

### 6.4 逾期管理

| 优先级 | 检查项 | 验收标准 | 结论 | 证据 / 备注 |
| --- | --- | --- | --- | --- |
| P0 | 所有账单 | 账单列表、筛选、详情正常 | 本地通过 | 本地接口 `/blade-ics/payment/page` 返回 `56` 条收款账单，`/blade-ics/payment/detail?paymentId=12` 详情正常；已在 `bills-all.vue` 增加对方名称/操作列详情入口和右抽屉，SFC 编译通过。 |
| P0 | 逾期账单 | 逾期金额、逾期天数、状态准确 | 本地通过 | 本地库动态逾期未缴 `2` 条，`paymentId=12` 应收 `11100.00`、实收 `0.00`、需收 `11100.00`，`paymentId=8` 应收 `9768.00`、实收 `0.00`、需收 `9768.00`；两条 `payDeadline=2026-05-01`，按 `2026-07-14` 计算逾期 `74` 天，接口状态均返回 `payStatus=2`。 |
| P0 | 逾期提醒 | 租客右抽屉操作闭环正常 | 本地通过 | `/blade-ics/payment/overdue-reminder-page` 返回逾期提醒 `2` 条；前端 `overdue-reminder.vue` 租客/处理打开右抽屉，抽屉包含查看合同、记录催缴、通知文件生成/预览下载、小程序发送、律师函审批、退租审批、联动日志；`/blade-ics/payment/overdue-disposal-detail?paymentId=12` 返回 `workflowRecords=2`。 |
| P0 | 催缴记录 | 记录催缴后可在联动日志 / 催缴日志中查看 | 本地通过 | `/blade-ics/payment/overdue-log-list?contractId=20` 返回 `23` 条联动日志，包含 `action=remind`、`actionDesc=发起催缴提醒`；日志按钮受 `finance_overdue_reminder_log` 权限控制。 |
| P0 | 逾期审批 | 逾期一个月以上可发起项目 / 法务审批 | 本地通过 | 本地 `paymentId=8`、`paymentId=12` 均逾期 `74` 天，已有 `biz_contract_workflow_record.business_type=contract_overdue_legal` 记录，`process_def_key=law`、`form_key=project-approval-law`、`process_status=approved`、`current_node=流程结束`；右抽屉可发起逾期律师函审批。 |
| P0 | 通知下载 | 收款通知、付款通知、开票申请对应模板正确 | 代码已修复，待重启复验 | Word 下载接口均返回 docx：`project-approval`、`payment-notice`、`invoice-apply`、`reminder-notice`、`overdue-notice`、`legal-letter`、`move-out-notice` 文件头均为 `504b0304`；收款通知页按现有口径映射为《开票申请单》。已修复 `invoice-apply` 的 `物业` 字段兜底和预览可选字段口径，后端 `clean compile` 通过；需重启后复验预览缺失字段为 `0`。 |
| P1 | 通知状态 | 短信、邮箱、小程序发送状态入口清楚 | 代码已修复，待重启复验 | 收款通知页有短信、邮箱、小程序状态筛选与表格列；`/blade-ics/payment/notice-page` 返回 `57` 条通知候选，默认状态 `pending`；后端短信/邮箱接口当前按“通道未接入/缺少联系方式”回写 `failed`，小程序接口生成预留发送数据。真实短信/邮箱通道待生产配置；已将汇总卡“已生成”改为统计真实 `biz_payment_notice` 记录数，后端 `clean compile` 通过，需重启后接口复验。 |

### 6.5 企业服务

| 优先级 | 检查项 | 验收标准 | 结论 | 证据 / 备注 |
| --- | --- | --- | --- | --- |
| P0 | 企业服务菜单 | 页面可进入，无空白 | 本地通过 | 本地 `admin` 登录后顶部“企业服务”菜单可见，`/blade-system/menu/routes` 已返回企业服务菜单树；父级入口 `/enterprise/property-service`、`/enterprise/merchant-service` 以及叶子页面 `/enterprise/property-service-config`、`/enterprise/property-workorder`、`/enterprise/merchant`、`/enterprise/merchant-ad`、`/enterprise/merchant-service-process`、`/enterprise/policy-service`、`/enterprise/enterprise-data` 均可进入且非空白。已修复 Saber3 动态路由多级菜单父级默认页丢失问题。 |
| P1 | 物业 / 服务入口 | 现有功能可用或明确为占位 | 本地通过 | 后台接口冒烟均通过：物业服务配置、物业工单、服务商、服务办理、广告、政策服务、企业数据看板接口均返回 `success=true/code=200`；本地业务数据当前为 `0` 条。实际功能页可用，服务商广告/服务处理为已预留业务入口；父级入口页已复验可进入。 |
| P1 | 小程序关联 | 涉及小程序入口的页面路径和数据接口明确 | 本地通过，待小程序端联调 | 小程序相关接口本地均返回 `success=true/code=200`：`/blade-ics/property-service/miniapp/list`、`/blade-ics/merchant/miniapp/list`、`/blade-ics/merchant-ad/miniapp/list`、`/blade-ics/policy-service/miniapp/list`、`/blade-ics/merchant-service-order/miniapp/my-page`、`/blade-ics/property-workorder/miniapp/my-page`、`/blade-ics/property-workorder/miniapp/admin/page`。当前仅验证后台接口和路径，真实小程序登录态、入口跳转与端侧展示仍需小程序端联调。 |

### 6.6 协同办公

| 优先级 | 检查项 | 验收标准 | 结论 | 证据 / 备注 |
| --- | --- | --- | --- | --- |
| P0 | 我的事务 | 待办、已办、抄送、发起列表正常 | 本地通过 | 本地 `admin` 通过浏览器登录后复验协同办公页面：`/plugin/workflow/pages/process/start`、`todo`、`my-done`、`done`、`send`、`copy`、`claim` 均可进入，页面标题命中且左侧为“协同办公”。接口均返回 `success=true/code=200`：可发起流程 `7` 条、待办 `0` 条、待签 `0` 条、我的请求 `19` 条、办结事宜 `17` 条、我的已办 `1` 条、抄送 `0` 条。 |
| P0 | 审批处理 | 同意、驳回、转审、查看详情正常 | 部分通过，待真实审批动作复验 | Workflow 收费组件接口与入口存在：`/blade-workflow/process/detail` 对合同审批实例返回 `success=true`，含 `button/process/form/flow`，流转节点 `5` 条；流程运维页 `/plugin/workflow/pages/ops/index`、`list`、`done` 均可进入，`/blade-workflow/ops/list` 返回待办任务 `2` 条，前端已接入通过、驳回、转办、委托、终止等按钮和接口。为避免改动当前本地业务数据，本轮未实际执行同意/驳回/转审，需要用专门测试流程造数后复验。 |
| P0 | 流程状态 | 审批中、已完成、被驳回、已撤回准确 | 部分通过，状态文案需收口 | Workflow 接口返回状态正常，样例合同审批 `processIsFinished=finished`，我的已办样例 `status=done`；页面状态字典覆盖 `unfinished/finished/terminate/withdraw/recall/reject/deleted`。但原生页面仍显示“进行中 / 已终结 / 已撤销”等 BladeX Workflow 默认文案，未完全收口为业务口径“审批中、已完成、被驳回、已撤回”；且本地当前无待办/驳回/撤回样例，需造数复验。 |
| P0 | 流转信息 | 流程审批人与意见能被业务表单回流 | 本地通过，待标准多节点复验 | 后端已接入 `WorkflowApprovalTraceService.approvalFields(processInsId)`，从 Flowable 历史任务、审批意见、流程发起人、用户姓名、角色、部门生成“部门审批/分管领导/总经理/审批流转信息”等字段；合同模板通过 `ContractWorkflowTraceService` 回流，入驻审批表通过 `BusinessOpportunityServiceImpl.exportTenantEntryApprovalForm` 回流。本地第 8 节样例可证明单节点/简化流程能用流程发起人兜底回流；正式“招商/财务/部门领导/分管领导”多节点仍需按配置实际审批后复验对应栏位。 |

## 7. 流程专项验收

| 优先级 | 流程 | 验收标准 | 结论 | 证据 / 备注 |
| --- | --- | --- | --- | --- |
| P0 | 入驻审批 | 真实账号审批后，企业入驻审批表回流审批人、意见、时间 | 部分通过，待标准节点复验 | 本地样例 `opportunityId=6`，流程实例 `dd018a21-742a-11f1-a373-22d9abd8c69f` 已办结；导出 `/blade-park/opportunity/tenant-entry/approval-form/6?processInsId=...` 返回 Office 文件，XML 文本可见 `管理员`、`同意`、审批表标题，说明回流链路可用。当前历史流程只有 `发起人 + 空节点名` 两个任务，不能证明“部门领导 / 分管领导”等标准节点均可回填。 |
| P0 | 合同审批 | 合同会签审批表、合同正文可带合同字段和审批信息 | 本地通过，待标准多节点复验 | 本地样例 `contractId=20`；`contract-approval`、`contract-fixed`、`contract-floating` 预览均 `HTTP 200/code=200/missingFields=0`，下载均为 docx。合同正文字段可生成；合同会签审批表下载文件可见 `总经理审批：老板（同意，2026-07-02）`，简化流程可用流程发起人兜底回流。部门经理、律师意见、分管领导等标准多节点栏位需正式流程节点配置后复验。 |
| P0 | 付款审批 | 付款通知单字段和审批信息准确 | 部分通过，待多节点付款流程复验 | 本地样例 `paymentId=10`，付款通知预览 `HTTP 200/code=200/missingFields=0`，下载 docx 正常；预览字段包含 `总经理审批=老板（同意，2026-07-02）`、`审批流转信息=2026-07-02 16:56:36 发起人 老板：同意`。当前样例实际只有单任务节点，仍需用财务/领导标准节点流程复验。 |
| P0 | 开票审批 | 开票申请单字段和审批信息准确，不串付款流程 | 本地通过，待标准多节点复验 | 本地样例 `paymentId=11`，`invoice-apply` 预览 `HTTP 200/code=200/missingFields=0`，下载 docx 正常，当前接口不串付款通知。已执行 `28_fix_invoice_workflow_print_file_url.mysql.sql` 修复旧流程记录 `record_id=10/paymentId=7/process_def_key=invoice`，`print_file_url` 已改为 `/blade-contract/print/invoice-apply/7`，开票流程错误打印地址数量为 `0`。本次修复后下载文件可见 `分管领导：老板（同意，2026-07-02）`，简化流程可用流程发起人兜底回流；标准财务/领导节点仍需复验。 |
| P0 | 逾期法务审批 | 项目审批、催款、逾期通知、律师函、限期搬离可生成 | 本地通过，审批意见数据需复核 | 本地样例 `paymentId=12`，法务流程实例 `179c5fef-75f4-11f1-b839-dea2d50ecd0c` 已办结，历史任务含 `发起人 + 老板`；`project-approval`、`reminder-notice`、`overdue-notice`、`legal-letter`、`move-out-notice` 预览均 `HTTP 200/code=200/missingFields=0`，下载 docx 正常。项目审批表可见 `总经理=老板`；但 Flowable 历史意见表中该节点意见本身存为 `??`，导出只是事实回流，意见内容需要重新用真实意见复验。 |
| P0 | 退租审批 | 退租审批表、合同解除补充协议可生成 | 本地通过，待标准多节点复验 | 本地样例 `contractId=20`，退租流程实例 `fa50df61-75f4-11f1-80f8-4a75d5c8ae12` 已办结；`termination-approval`、`termination-agreement` 预览均 `HTTP 200/code=200/missingFields=0`，下载 docx 正常。退租审批表下载文件可见 `总经理：老板（同意，2026-07-02）`，简化流程可用流程发起人兜底回流；分管领导、财务等标准节点仍需复验。 |
| P0 | 房屋交接验收 | 房屋退租交接验收单可生成 | 本地通过，待标准多节点复验 | 本地样例 `contractId=20`，房屋验收流程实例 `07ac0ffa-75f5-11f1-80f8-4a75d5c8ae12` 已办结；`room-review` 预览 `HTTP 200/code=200/missingFields=0`，下载 xlsx 正常。下载文件可见 `招商部签字：老板（同意，2026-07-02）`，简化流程可用流程发起人兜底回流；物业/运营/招商多节点仍需复验。 |

## 8. 原模板打印专项

| 优先级 | 模板 | 字段完整 | 空值兜底 | 回填位置 | 预览 | 下载 | 版式一致 | 结论 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| P0 | 合同会签审批表 | 通过 | 通过 | 通过 | 通过 | 通过 | 通过 | 1 页；原表 13 行、25 单元格不变，真实审批人及意见已回流 |
| P0 | 合同正文（固定租金版） | 通过 | 通过 | 通过 | 通过 | 通过 | 通过 | 11 页；日期、承租方、房源、租期等字段按原混合样式和下划线位置回填 |
| P0 | 合同正文（浮动租金版） | 通过 | 通过 | 通过 | 通过 | 通过 | 通过 | 11 页；字段回填、分页、附表和签章区与原模板结构一致 |
| P0 | 付款通知单 | 通过 | 通过 | 通过 | 通过 | 通过 | 通过 | 2 页；第 1 页落款完整，第 2 页保持原模板空白页 |
| P0 | 开票申请单 | 通过 | 通过 | 通过 | 通过 | 通过 | 通过 | 2 页；“开票内容及/房租”在第 1 页，“所属期”和审批栏在第 2 页，真实审批人已回流 |
| P0 | 催款通知书 | 通过 | 通过 | 通过 | 通过 | 通过 | 通过 | 2 页；费用表保持 7 行、41 单元格，收件人、房源、日期和金额均按原位置回填 |
| P0 | 租金逾期处理通知书 | 通过 | 通过 | 通过 | 通过 | 通过 | 通过 | 2 页；正文条款、日期、金额和落款版式与原模板结构一致 |
| P0 | 退租审批表 | 通过 | 通过 | 通过 | 通过 | 通过 | 通过 | 2 页；第 1 页标题页、第 2 页审批表完整保留，真实审批人已回流 |
| P0 | 合同解除补充协议 | 通过 | 通过 | 通过 | 通过 | 通过 | 通过 | 3 页；正文、双方签章及日期分页与原模板结构一致 |
| P0 | 房屋退租交接验收单 | 通过 | 通过 | 通过 | 通过 | 通过 | 通过 | `.xlsx` 下载、2 页 PDF 预览；合并单元格、打印区和审批签字位置正确，无错误空值写入 |

#### 2026-07-15 第 8 节本地验收记录

- 复验结论：10 个模板当前 `0/10` 可以签署“100% 一致”。按下载文件视觉与 OOXML 结构分组：合同会签审批表 `1` 个基本通过；合同正文固定版、浮动版、租金逾期处理通知书、合同解除补充协议 `4` 个部分通过；付款通知单、开票申请单、催款通知书、退租审批表、房屋退租交接验收单 `5` 个不通过。
- 本地接口样例：`contractId=20`，付款通知 `paymentId=10`，开票申请 `paymentId=11`，催款/逾期通知 `paymentId=12`。本轮重新调用 10 个下载接口，均返回 `HTTP 200` 和正确 Office MIME 类型；其中 9 个为 `.docx`，房屋退租交接验收单为 `.xlsx`，均不是 PDF。
- 字段级结果：10 个 `/blade-contract/print/preview` 均返回 `HTTP 200/code=200/missingFields=[]`；下载文件内部未发现 `{{...}}`、`${...}`、`__exact__:`、`null`、`undefined` 残留。该结果只能证明本地样例字段已处理，不能证明版式一致。
- 下载分页对比：原模板/生成文件分别为：合同会签 `1/1`、固定正文 `11/11`、浮动正文 `11/11`、付款通知 `2/2`、开票申请 `2/2`、催款通知 `2/2`、逾期通知 `2/2`、退租审批 `2/1`、解除协议 `3/3`、交接验收 `2/2`。页数相同的文件仍存在表格结构、内容跨页或样式偏差。
- 结构证据：催款通知原模板为 `7` 行、`41` 个单元格，生成文件为 `3` 行、`17` 个单元格；固定/浮动合同正文生成文件文本运行数分别从 `371/376` 降为 `326/324`；开票申请从 `42` 降为 `31`；催款通知从 `157` 降为 `58`；逾期通知从 `48` 降为 `16`；解除协议从 `98` 降为 `18`。整段替换会保留首个运行样式，但无法保留原段内的下划线、字号、加粗等混合样式。
- 审批回流现状：合同会签、开票申请、退租审批、房屋交接验收下载文件可见 `老板（同意，2026-07-02）`，说明简化流程的数据回流链路可用；正式“招商/财务/部门领导/分管领导”多节点尚未按最终节点配置完成真实审批，不能作为 100% 验收依据。
- 预览限制：当前 `/preview` 是 POI 解析后的 HTML 近似预览，不是 Word/WPS/LibreOffice 原生渲染。固定/浮动合同 HTML 预览为 `8` 个 `.word-page`，下载 Word 实际均为 `11` 页，因此预览与下载打印不能认定 100% 一致。
- 复验证据目录：当前下载文件 `output/contract-acceptance/8-recheck/current/`；渲染页 `output/contract-acceptance/8-recheck/current-rendered/`；原模板与生成文件逐页对照图 `output/contract-acceptance/8-recheck/pairs-current/`。

#### 2026-07-16 第 8 节修复后验收记录

- 最终结论：按当前 10 个原模板、当前本地业务样例和已部署流程定义验收，`10/10` 通过。下载仍为 9 个 `.docx` 和 1 个 `.xlsx`；预览由同一份生成后的 Office 字节经 LibreOffice 转为 PDF，不再使用近似 HTML 排版。
- 真实接口结果：合同 `contractId=20`，付款通知 `paymentId=10`，开票申请 `paymentId=11`，催款/逾期通知 `paymentId=12`。10 个下载接口、10 个预览接口均返回 `HTTP 200`，全部 `missingFields=[]`。
- 最终预览页数：合同会签 `1`、固定正文 `11`、浮动正文 `11`、付款通知 `2`、开票申请 `2`、催款通知 `2`、逾期通知 `2`、退租审批 `2`、解除协议 `3`、交接验收 `2`，与原模板页数逐项一致。
- 字段与结构门禁：Office 文件中无 `{{...}}`、`${...}`、`__exact__:`、`null`、`undefined` 残留；Word 表格行列数保持原模板结构；混合字号、下划线、加粗运行采用样式保留替换；Excel 合并单元格和打印区域未改变。
- 审批回流：当前样例流程的真实账号“老板”及完成日期、审批意见已回填到合同会签、开票申请、退租审批、房屋交接验收等具备审批栏位的模板；Flowable 历史脏意见 `??` 已按已完成审批兜底为“同意”，不可读字符不会进入打印文件。
- 预览与打印：预览 PDF 直接来自本次生成的 Office 文件，页面、分页和下载文件一致；前端打印直接打印内嵌 PDF，不再对预览 HTML 二次排版。
- 最终证据：下载文件 `output/contract-acceptance/8-recheck/final-current/`；预览 PDF `output/contract-acceptance/8-recheck/final-preview-pdf/`；逐页渲染 `output/contract-acceptance/8-recheck/final-preview-rendered/`；原模板对照 `output/contract-acceptance/8-recheck/pairs-final/`；Excel 结构/视觉检查 `output/contract-acceptance/8-recheck/xlsx-rendered/`。
- 部署前置：测试/生产服务器必须安装 LibreOffice（`soffice` 可执行）及完整中文字体，推荐 Noto CJK/思源字体；可通过 `blade.contract.preview.soffice-command` 和 `blade.contract.preview.timeout-seconds` 覆盖命令路径与超时时间。流程节点或模板文件后续变更时，需要重新执行本节验收。

## 9. 通知链路验收

| 优先级 | 检查项 | 验收标准 | 结论 | 证据 / 备注 |
| --- | --- | --- | --- | --- |
| P0 | 通知策略确认 | 明确短信、邮箱、小程序是否正式发送 | 部分通过 / 待产品确认 | 当前实现口径明确：短信、邮箱未接真实第三方，调用后记录失败原因；小程序为预留发送数据；收款通知重发会标记站内信成功、小程序成功。上线前需产品确认“短信/邮箱先不真实发送”是否可上线 |
| P0 | 发送记录 | 每次发送有记录、状态、失败原因 | 本地通过 | 2026-07-15 本地 `paymentId=12` 验证：`biz_payment_notice.notice_id=2`，`sms_status=failed`、`email_status=failed`、`inbox_status=success`、`miniapp_status=success`、`send_count=3`，`remark=缺少手机号；缺少邮箱；站内信已发送；小程序发送记录已生成` |
| P0 | 发送失败兜底 | 第三方失败时页面状态明确，不影响主流程 | 本地通过 | 调用 `/blade-ics/payment/notice-sms-send`、`/notice-email-send` 返回 200 并落失败状态；调用 `/blade-ics/payment/overdue-reminder-remind` 返回 200，账单 `remind_status=1`，合同日志新增 `payment_notice_sms`、`payment_notice_email`、`payment_notice_miniapp`、`notice_miniapp`、`remind` |
| P1 | 模板内容确认 | 短信、邮箱、小程序文案通过业务确认 | 部分通过 / 待业务确认 | 小程序 payload 可生成，`/blade-contract/print/miniapp/send?noticeType=overdue-notice&paymentId=12&contractId=20` 返回 `sendStatus=pending`、`channel=miniapp`、`businessType=contract_overdue_notice`、`sendMode=reserved`；短信/邮箱当前只有失败提示文案，真实发送模板需业务确认 |
| P1 | 接收人规则 | 招商员、部门领导、分管领导、财务接收规则明确 | 部分通过 / 待最终流程配置复验 | 收款/逾期通知当前接收人为合同客户联系人及小程序 openId/unionId；招商员、部门领导、分管领导、财务属于 Workflow 审批节点接收人，不在 `biz_payment_notice` 中沉淀。最终流程节点配置完成后需用真实账号复验 |

#### 2026-07-15 第 9 节通知链路本地验收记录

本地后端：`http://127.0.0.1:8080`，登录账号：`admin`，样本账单：`paymentId=12`，合同：`contractId=20`。

接口结果：

| 接口 | 结果 | 备注 |
| --- | --- | --- |
| `/blade-ics/payment/notice-page?current=1&size=10` | 通过 | 返回 57 条账单通知视图，未生成记录默认状态为 `pending` |
| `/blade-ics/payment/notice-summary` | 通过 | 重发后汇总：`generatedCount=1`、`inboxSuccessCount=1`、`miniappSuccessCount=1` |
| `/blade-ics/payment/notice-sms-send?paymentId=12` | 通过 | 返回 200，落 `smsStatus=failed`，失败原因：缺少手机号 |
| `/blade-ics/payment/notice-email-send?paymentId=12` | 通过 | 返回 200，落 `emailStatus=failed`，失败原因：缺少邮箱 |
| `/blade-ics/payment/notice-miniapp-send?paymentId=12` | 通过 | 返回 200，落 `miniappStatus=success` |
| `/blade-ics/payment/notice-generate?paymentId=12` | 通过 | 生成《开票申请单》docx；本次先清理 Redis OSS 缓存 `000000:blade:resource::oss:code:000000` 后通过 |
| `/blade-ics/payment/notice-resend?paymentId=12` | 通过 | 返回 200，落 `inboxStatus=success`、`miniappStatus=success`，更新文件地址 |
| `/upload/bladex/upload/20260715/8208a2b577379fc47620dd66c51545d8.docx` | 通过 | 登录态下载 HTTP 200，`bytes=10887`，文件头 `504b0304`，确认为 docx |
| `/blade-contract/print/miniapp/send?noticeType=overdue-notice&paymentId=12&contractId=20` | 通过 | 返回小程序预留 payload，`sendStatus=pending`、`sendMode=reserved` |
| `/blade-ics/payment/overdue-reminder-remind?paymentId=12` | 通过 | 返回 200，账单催缴状态更新为已催缴 |
| `/blade-ics/payment/overdue-disposal-detail?paymentId=12` | 通过 | 返回 `paymentNotice`、`miniAppRecords`、`workflowRecords`，可在逾期提醒抽屉查看链路记录 |

修复记录：

- 本地 OSS 缓存曾指向默认 MinIO，导致 `/notice-generate`、`/notice-resend` 上传文件时报 `127.0.0.1:9000` 连接失败。已清理 Redis 资源缓存后，系统按 `blade_oss` 启用的 `local` 配置生成文件。
- 新增后端 `/upload/**` 静态资源映射到 `Boot/uploadPath/`，修复本地 OSS 生成文件可落盘但 URL 下载 500 的问题。

上线注意：

- 测试/生产环境部署后需确认 `blade_oss` 启用记录、Redis OSS 缓存、文件访问域名三者一致；切换 OSS 后需清理资源缓存或重启服务。
- 短信、邮箱如要正式发送，需补真实通道配置、模板编号、发送回执处理和失败原因回写；当前实现是“可点击、可记录失败、不阻断主流程”。
- 小程序当前是预留 payload，不是微信订阅消息正式发送；正式发送需接入小程序服务端接口并补 openId/unionId 数据。

## 10. 安全与合规

| 优先级 | 检查项 | 验收标准 | 结论 | 证据 / 备注 |
| --- | --- | --- | --- | --- |
| P0 | Token 机制 | 登录、刷新 token、严格请求头、无 token 拦截正常 | 本地通过 | 本地 `admin` 账号登录 `/blade-auth/oauth/token` 返回 200，密码走 SM2 加密；`refresh_token` 返回 200；业务接口无 `Blade-Auth` 返回 401，缺 `Blade-Requested-With` 返回 401，有效 token 返回 200。`BladeConfiguration` 已启用 `strictTokenEnabled()`、`strictHeaderEnabled()`。 |
| P0 | 参数签名 | 关键写接口具备签名校验或明确不上线启用范围 | 不通过 / 待决策 | `BladeConfiguration` 当前调用 `signDisabled()`，只保留了示例签名规则 `/blade-desk/dashboard/sign`，未对合同、账单、审批、附件等业务接口启用请求签名。若上线要求“参数签名”，需先定义签名范围、前端签名算法和灰度开关，否则不能直接开启全局签名。 |
| P0 | AI 后端安全 | AI 能力只能由后端代理，密钥不得出现在前端或仓库 | 当前不适用 / 待未来复验 | 代码扫描未发现 OpenAI、DeepSeek、LLM、prompt 等 AI 后端接口或前端密钥引用；当前项目未接入 AI 后端。后续如接入，必须后端代理、服务端环境变量保存密钥、接口走 token/权限/频控/日志，不允许前端直连模型服务。 |
| P0 | 接口权限校验 | 核心业务接口有 token 和菜单/角色权限，普通账号不能越权 | 部分通过 | 合同、逾期、园区、企业服务、协同办公等业务 Controller 多数有类级或方法级 `@PreAuth(menu=...)`；日志、OSS、短信等系统接口使用 `@IsAdmin`。本地实测合同列表、日志列表无 token 均 401，有效 admin token 200。待补：普通角色账号越权访问合同、账单、日志的实测；`SearchController` 仅受 token/严格请求头保护，没有菜单级 `@PreAuth`，需确认是否允许所有登录用户查人员/部门。 |
| P0 | 敏感配置不提交 | 数据库密码、Redis 密码、OSS 密钥、短信密钥、token 密钥不进入 Git | 不通过 | `application-dev.yml/test.yml/prod.yml` 和部署文档中仍存在明文数据库、Redis、OSS 配置；`application.yml` 中 OAuth2 SM2、token sign-key、crypto-key 为代码内静态值。上线前需改为环境变量、外部配置或服务器私有配置，并重新生成生产密钥。 |
| P0 | 默认密码处理 | 生产账号无默认弱密码，前端不预填弱密码 | 不通过 / 待修复 | 本地 `admin` 实际可用密码已不是默认 `admin`；但前端登录页仍预填 `username=admin`、`password=admin`。上线包建议清空默认账号密码，并复核生产用户是否存在 `admin/admin`、`123456` 等弱密码。 |
| P0 | 数据库不暴露公网 | MySQL、Redis、OSS 仅内网访问或有安全组限制 | 待服务器确认 | 本地和配置文件使用 `127.0.0.1`、`192.168.*`、`172.*` 等内网地址，但是否暴露公网取决于测试/生产服务器安全组、防火墙、Docker 端口映射和 Nginx 反代配置，需在服务器执行端口扫描和安全组复核。 |
| P0 | 文件下载鉴权 | 合同、审批表、账单、附件下载必须登录鉴权 | 本地通过 | 本地实测 `/blade-contract/print/contract-approval/20` 无 token 返回 401，有效 token 返回 200 且为 docx 文件流。新增 `/upload/**` 本地静态映射后仍需通过登录态访问业务生成的文件 URL；测试/生产需复验 OSS 文件域名是否被匿名公开。 |
| P0 | 越权访问验证 | 普通账号不能查看无权限合同、账单、日志 | 部分通过 / 待普通账号复验 | 本轮完成“未登录拦截”和“缺严格请求头拦截”；缺少招商、财务、领导等普通账号口令，未完成跨角色越权实测。需用真实角色账号分别验证合同、账单、联动日志、系统日志、附件下载。 |
| P1 | API 报文加密 | 敏感接口按需启用 API 加解密或确认仅依赖 HTTPS + Token | 未启用 / 待决策 | 已引入 `blade-starter-api-crypto`，但 `blade.api.crypto.enabled=false`，代码中未发现 `@ApiCrypto/@ApiEncrypt/@ApiDecrypt`。如上线环境强制 HTTPS 且不要求报文二次加密，可记录为“暂不启用”；否则需按登录外的敏感写接口逐步接入。 |
| P1 | 操作日志 | 关键增删改、审批、导出有日志 | 部分通过 / 待补强 | `blade_auth_log` 本地有认证日志 328 条，`blade_log_error` 有错误日志 98 条；`blade_log_api`、`blade_log_usual` 当前为 0，业务模块未发现 `@ApiLog`。合同/逾期存在业务日志和 Workflow 流程记录，但通用 API 操作日志未覆盖关键增删改、导出、审批按钮，需补 `@ApiLog` 或统一日志切面策略。 |

#### 2026-07-15 第 10 节安全与合规本地验收记录

- Token 实测：`/blade-auth/oauth/token` 登录 200，`grant_type=refresh_token` 200；`/blade-contract/contract/list` 无 token 401，有 token 200，缺 `Blade-Requested-With` 401。
- 文件下载实测：`/blade-contract/print/contract-approval/20` 无 token 401，有 token 200，返回 Office 文件流。
- 日志入口实测：`/blade-log/api/list` 无 token 401，有 admin token 200。
- 代码扫描：业务 Controller 大多使用 `@PreAuth(menu=...)`，系统日志/OSS/SMS 使用 `@IsAdmin`；未发现业务模块使用 `@ApiLog` 或 `@ApiCrypto`。
- 本地库统计：`blade_auth_log=328`，`blade_log_error=98`，`blade_log_api=0`，`blade_log_usual=0`，`blade_scope_api=0`，`blade_scope_data=1`。
- 风险收口：参数签名未启用、敏感配置明文提交、前端登录页预填默认弱密码、普通角色越权验证未完成，这 4 项不应放到上线后处理。

## 11. 性能与稳定性

| 优先级 | 检查项 | 验收标准 | 结论 | 证据 / 备注 |
| --- | --- | --- | --- | --- |
| P1 | 首页加载 | 常规网络下首页首屏可接受 | 本地通过 | Playwright 打开 `/desk`，`loadMs=382ms`，`domContentLoadedMs=380ms`，页面命中首页内容，console 无 error；截图 `output/playwright/11-performance-home-desk-20260715.png`。后端聚合接口 `/blade-home/home/workbench` 3 次平均 `63ms`，最大 `116ms`。 |
| P1 | 列表分页 | 大数据量列表分页、筛选无明显卡顿 | 本地通过，楼层优化待页面复测 | 本地小数据量：合同 `11` 条、账单 `57` 条、楼层 `38` 条。接口 3 次平均：合同列表 `74ms`，所有账单 `164ms`，逾期账单 `52ms`，园区列表 `39ms`，工作流可发起流程 `99ms`，工作流待办 `20ms`，工作流运维待办 `57ms`。楼层列表优化前 `/blade-park/floor/list` 平均 `806ms`、最大 `996ms`；已将逐楼层房间查询改为一次批量查询，租控楼层点击不再重复返回完整园区树，并补 `ics_room` 园区、楼层组合索引。按用户要求不代登录测试，待页面复测回填优化后耗时。 |
| P1 | 文件生成 | Word / Excel / PDF 生成不超时 | 本地通过 | 3 次平均：合同正文固定版预览 `/blade-contract/print/preview?noticeType=contract-fixed&contractId=20` 为 `549ms`、最大 `1153ms`；合同正文固定版下载 `135ms`；开票申请单下载 `73ms`；房屋退租交接验收单下载 `102ms`。当前均返回 200，未超时。 |
| P1 | 并发审批 | 多人同时审批不出现重复处理或状态错乱 | 部分通过 / 待专项压测 | 本轮只做非破坏性并发读冒烟：`/blade-contract/contract/list` 20 次并发、`/blade-home/home/workbench` 20 次并发，均 `20/20` 返回 200。未实际执行多人同时同意/驳回同一任务，避免改动当前业务数据；上线前需专门造流程任务，用招商/财务/领导多账号做并发审批专项。 |
| P2 | 监控告警 | CPU、内存、磁盘、服务存活有监控 | 不通过 / 待接入 | 本地 `/actuator/health`、`/actuator/prometheus` 均返回 404；`Boot/pom.xml` 未发现 `blade-starter-metrics` 或 Prometheus/Micrometer 相关依赖。上线前需接入服务健康检查和 Prometheus 指标，服务器侧补 NodeExporter、MySQL/Redis/Docker 监控与告警规则。 |

#### 2026-07-15 第 11 节性能与稳定性本地验收记录

- 本地后端：`http://127.0.0.1:8080`，前端：`http://localhost:2888`，账号：`admin`。
- 本轮为本地小数据量冒烟，不是生产压测；未执行 `npm run build`。
- 数据规模：`biz_contract=11`、`biz_contract_payment=57`、`ics_park=2`、`ics_building=2`、`ics_floor=38`、`ics_room=2`、`biz_contract_workflow_record=18`。
- API 耗时采用 `curl %{time_total}`，每个接口 3 次取平均和最大值；前端首页采用 Playwright 浏览器导航性能数据。
- 风险项：楼层列表已完成查询收敛与索引优化，待页面复测优化后耗时；监控告警未接入；并发审批还缺真实多账号、多任务专项。

## 12. 打包与部署演练

| 优先级 | 检查项 | 验收标准 | 结论 | 证据 / 备注 |
| --- | --- | --- | --- | --- |
| P0 | 后端正式打包 | 生成可运行 jar，启动无报错 | 待验收 | 包名 |
| P0 | 前端正式打包 | 生成 dist，刷新路由正常 | 待验收 | 包名 |
| P0 | 测试环境按生产方式部署 | 不依赖本地代理、不依赖开发服务 | 待验收 | 访问地址 |
| P0 | 数据库备份演练 | 可备份、可恢复测试库 | 待验收 | 备份恢复记录 |
| P0 | 回滚演练 | 能回滚到上一版 jar / dist / 数据库 | 待验收 | 演练记录 |
| P1 | 部署耗时记录 | 明确停机窗口和预计恢复时间 | 待验收 | 时间记录 |

## 13. 上线当天执行清单

| 顺序 | 操作 | 负责人 | 命令 / 说明 | 结果 |
| --- | --- | --- | --- | --- |
| 1 | 通知业务停用系统 |  |  |  |
| 2 | 备份数据库 |  |  |  |
| 3 | 备份上传文件 / 附件目录 |  |  |  |
| 4 | 停止旧服务 |  |  |  |
| 5 | 执行生产 SQL |  |  |  |
| 6 | 部署后端 jar |  |  |  |
| 7 | 部署前端 dist |  |  |  |
| 8 | 重启服务 |  |  |  |
| 9 | 验证登录和首页 |  |  |  |
| 10 | 验证核心业务链路 |  |  |  |
| 11 | 验证流程审批和文档导出 |  |  |  |
| 12 | 开放业务使用 |  |  |  |

## 14. 上线后观察

| 时间点 | 检查项 | 验收标准 | 结果 | 备注 |
| --- | --- | --- | --- | --- |
| 上线后 30 分钟 | 服务状态 | 后端、前端、数据库、Redis 正常 | 待观察 |  |
| 上线后 1 小时 | 错误日志 | 无大量 500、SQL 异常、鉴权异常 | 待观察 |  |
| 上线当天 | 核心业务 | 登录、首页、合同、逾期、审批正常 | 待观察 |  |
| 上线后 1 天 | 业务反馈 | 无阻断业务的问题 | 待观察 |  |
| 上线后 1 周 | 遗留问题复盘 | 形成上线后优化清单 | 待观察 |  |

## 15. 上线阻断项记录

| 编号 | 问题 | 影响范围 | 优先级 | 负责人 | 计划修复时间 | 状态 |
| --- | --- | --- | --- | --- | --- | --- |
|  |  |  |  |  |  |  |

## 16. 上线豁免记录

| 编号 | 豁免项 | 豁免原因 | 风险说明 | 业务确认人 | 技术确认人 | 关闭时间 |
| --- | --- | --- | --- | --- | --- | --- |
|  |  |  |  |  |  |  |
